# 总管汇总：A（CA2 R2）与 B（超参数）及下一步

- 日期：2026-08-21
- 角色：总管汇审，不是查找复跑，不是修复
- 本文件默认 `pending_review`，不登记为 active 结论，不授权训练

## Locus（本次总管新鲜核验）

| 项 | 值 |
|---|---|
| branch | `main`（`origin/main` ahead 4） |
| HEAD | `469e5ef3c6d079844d32e775cd6edd7b3a9e15ad` |
| dirty | 仅未跟踪 `03审计报告/` |
| `state_revision` | 243 |
| `start-check --strict` | PASS=13 WARN=1 FAIL=0（barcode WARN） |

A/B 正文仍在本地 `03审计报告/`，未纳入 Git。

## 可检验命题

在 MPP2 修复基线及其后 Huber / Ridge / W007 上：

1. 是否还可把外部 PCC≈0.65 首先归因于一般性硬 Bug？
2. 超参数是否已达到“必须先改再谈划分”的程度？
3. 下一允许动作是什么？

## 证据分类

| 材料 | 类 |
|---|---|
| Registry 中修复基线 / Huber / W007 | `accepted` |
| Ridge r003 | `rejected` |
| CA2 R2 报告与审批回传 | `pending_review`（查找已完成，用户尚未在回传上勾选） |
| 超参数审查报告 | `pending_review`（门禁当时 FAIL=2，表依据未漂的 experiment_registry） |
| 上一轮 CA2 阻塞稿 | `historical` / 不作结论 |
| 本总管 start-check 与 `zscore_manifest.json` / `split_info.json` 抽查 | `current` |

抽查：`group_2/zscore_manifest.json` 的 `fit_split=train`，六训练患者，排除 internal_val / external_test；`split_info.json` 的 `val_ratio_target=0.1`。与 CA2 R2 的 CA2-2/CA2-3 协议描述一致。本 checkout 没有 `baseline_replay_check.json`（CA2 引用 quarantine/accepted bundle）；复放数字以 Registry 与 CA2 引用的 accepted 产物为准，总管标 WARN、不改硬 Bug 判定。

## A. CA2 R2

总体：`CONDITIONAL GO`。六项**硬 Bug 全 PASS**，无必须先修的训练代码。

| 项 | 硬 Bug | 协议 WARN |
|---|---|---|
| CA2-1 对齐 | PASS | 旧 in-repo 标签仍冲突；accepted 绑定 repaired staging |
| CA2-2 z-score | PASS | 优化在 train-z，raw 由同一套参数反变换 |
| CA2-3 患者/切片 | PASS | 六患者身份同时在 train 与 val（当时就是空间 10% 协议） |
| CA2-4 复放 | PASS | 选模用 internal val_loss，相对 XZY 可能偏乐观 |
| CA2-5 新参数 | PASS | 预测有差，外部 PCC 变化很小 |
| CA2-6 XZY 选模 | PASS | 未用 XZY 选模；internal-val 与 train 共享患者身份 |

因此：**停止把 0.65 首先归因于错配、z-score 泄漏、违反当时协议的切片泄漏、无法复放、新头未训练、或用 XZY 选模。**

## B. 超参数

审查完成度当时为 `CONDITIONAL GO`，训练授权 `NOT APPLICABLE`。没有达到“不合理三条件、必须先改超参”的项。

存疑、写入后续诊断/讨论稿即可，**现在不另开调参实验**：

- Huber δ=1 在 z 空间接近 MSE
- Ridge 选 λ=10、仅 1/30 通路启用；该跑次保持 `rejected`
- W007 对比权重 0.1、用回归指标选模：HCR 对比降、回归不动、选 epoch 1
- LoRA r=8 维持 `no_go_formal`

冻结基线的 lr / epoch / patience / min_delta / val_loss 选模：**合理**，不能把 0.65 归咎于过早停止。

## 总管判定

- 对“停止怀疑一般性硬 Bug”：`CONDITIONAL GO`
- 对“现在修 MPP2 训练代码”：`NO-GO`
- 对“现在开 Huber/W007/LoRA 重训或改超参合同”：`NO-GO`
- 对“下一步开划分与 z-score 只读诊断（对话 C）”：`GO`（须用户接受本汇总；诊断仍只读，不生成新 PCC，不重做受保护资产）
- 对“现在写定版部署方案并绑定 W008”：`NO-GO`（等对话 C）

点名条件：barcode WARN；旧 train CSV 不得当训练输入；当前 split_manifest 与训练绑定 sha 字节不同（CA2 已说明无 patch 跨 split）；A/B 报告未登记 active。

## 下一步（只做这一件）

开对话 C：划分与 z-score 只读诊断。完成后交回总管，再决定是否在 `01_指南与解读/部署方案/` 写讨论稿。
