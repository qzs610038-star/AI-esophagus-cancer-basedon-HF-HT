# 审批回传：超参数审查

## 回传头部

- 任务：超参数审查
- 对话角色：查找，不是总管，不是修复
- 仓库 locus：`main` / HEAD `f02d528c49dad95b3bbc3745e553c26c87723f47` / dirty=仅未跟踪 `03审计报告/`；相对 `origin/main` ahead 2
- `start-check --strict`：失败（PASS=11 WARN=1 FAIL=2）。失败项：`document_registry_sha256` 与 current_state 不一致；本地 prunable worktree `Temp\pfmval-w004-governance-f8a1c2f` 缺失。`experiment_registry_sha256` 一致。按提示词，本回传**不授权训练或状态修复**；超参表仍可作为总管只读汇审材料。
- 正式报告路径：`03审计报告/20260821_Phase2-MPP2-PCC瓶颈/超参数审查报告_20260821.md`
- 事实截止时间：2026-08-21（HEAD `f02d528`；Registry 与 job/回传产物现场读取）

## 可检验命题与判定

- 命题：MPP2 修复基线及 Huber / Ridge / W007 的登记超参，是否存在明显不合理、足以压住新增模块或造成过早停止/过拟合的设置（LoRA 只列表，不建议正式重跑）。
- 总体判定：审查完成度 `CONDITIONAL GO`；训练授权 `NOT APPLICABLE`
- 判定覆盖范围（一句话，写明没有覆盖什么）：覆盖上述实验合同、job 与训练日志中的超参合理性；不覆盖 CA2 硬 Bug、划分/z-score、W007 待审归因，也不给出新的 lr/δ/λ/对比权重处方。

## 分项结果

| 项 | 硬 Bug | 协议/设计 | 证据类 | 证据位置 |
|---|---|---|---|---|
| start-check --strict | 不适用 | FAIL（哈希与 stale worktree） | current | 本机命令输出；`git worktree list` |
| 冻结基线 lr/epoch/patience/min_delta/选模/batch/Adam | 不适用（本对话不判 CA2） | PASS（判定：合理） | accepted | job `mpp2-repair-v003-frozen-20260711-r002`；attempt-003 history/best_epoch |
| Huber δ=1 与对照全同 | 不适用 | WARN（δ=1 使 Huber≈MSE，存疑；对照全同为 PASS） | accepted | g12 合同；W001-A003/A004 argv 与 history |
| Ridge λ 网格 / LOPO / 冻结 | 不适用 | WARN（选 λ=10 且 1/30 通路启用，存疑）；状态 failed/rejected | rejected | r003 `calibrator.json`/`metrics.json`；Registry |
| W007 残差 lr / 对比权重 / 零初始化 / 早停 / 选模 / patient-balanced batch | 不适用 | WARN（HCR 对比降、回归不动、选 epoch 1，存疑；batch 与零初始化合理） | accepted（verification）；attribution pending_review 未用 | W007 合同+yaml+工作树代码；RCC/HCR/CPGCR `training_history.csv` |
| LoRA r=8 smoke 附表 | 不适用 | 已 no_go_formal；禁止建议正式重跑 | accepted smoke | S1 与 dropout10 job + history |

## 给总管的下一允许动作（只选一个）

- [ ] 等待用户审核本回传
- [ ] 授权另开修复代码对话（仅当硬 Bug FAIL）
- [ ] 停止怀疑一般性硬 Bug，允许开启划分与 z-score 诊断对话
- [x] 超参数审查完成，不授权训练；交总管决定是否纳入后续合同
- [ ] 划分/z-score 诊断支持总管去 `01_指南与解读/部署方案/` 写讨论稿（仍不绑定工作树）
- [ ] 证据不足，需要用户补充材料后再审

## 请用户当场批准

- [ ] 接受本回传的总体判定
- [ ] 接受“下一允许动作”
- [ ] 不接受；需要返工的点：

## 明确未做

- 未修代码、未训练、未 import/accept、未 commit、未 push
- 未判定 CA2 硬 Bug，未开划分/z-score 诊断
- 未用外部 XZY 反推“更好超参”，未建议 LoRA r=8 正式训练，未把 Ridge 失败跑次当校准器
- 未修复 start-check 的 document_registry 哈希或 stale W004 temp worktree
- 未写 `01_指南与解读/部署方案/` 讨论稿，未绑定工作树
