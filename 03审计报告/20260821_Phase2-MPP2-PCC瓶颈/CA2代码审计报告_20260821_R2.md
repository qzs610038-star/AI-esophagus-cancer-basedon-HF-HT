# PFMval independent audit

- 任务：CA2 代码审计 R2（只读）
- 对话角色：查找，不是总管，不是修复
- 报告日期：2026-08-21
- 与上一轮关系：`CA2代码审计报告_20260821.md` 只是 start-check 阻塞记录，总体 `NOT APPLICABLE`，六项均为 `missing`。**不得**把那份稿复述为本轮 PASS/FAIL。本文件为新稿，未覆盖旧阻塞稿。

## Claim

- Testable claim: 在 MPP2 修复基线及其后 Huber / Ridge / W007 实现中，CA2 规定的六类一般性硬 Bug 是否存在。若全不存在，可停止继续怀疑一般性硬 Bug。
- Requested decision: 对“可否停止怀疑一般性硬 Bug”给出判定；不覆盖划分改进、新训练、超参数再调，也不解释 PCC≈0.65 的科学上限。
- Scope and identifiers:
  - `mpp2_barcode_repair_v003_frozen_baseline_20260711`（accepted；result_id `mpp2-repair-v003-frozen-20260711-r002-result-20260711164140-f2b1b51d`；`result_manifest_sha256=dadd0c8322b05ed626faf00bae3743550d8d226891a00e05a63f7f84a487ce4f`）
  - `mpp2_huber_loss_paired_v001_20260728`（W001；accepted；`W001-A003-result-R004` MSE + `W001-A004-result-R002` Huber）
  - `mpp2_pathway_ridge_calibration_v001_20260717`（status=`failed`，evidence=`rejected`；审 r002/r003 冻结前是否读 XZY）
  - `mpp2_cpgcr_probe_v001_20260811`（W007；accepted；FBR/RCC/HCR/CPGCR）
  - 数据清单：`barcode-repair-20260711-d626ad8-v003:1204018178a4d355`
  - LoRA：不在本次 CA2 主审

## Locus

| 项 | 值 |
|---|---|
| branch | `main`（跟踪 `origin/main`，ahead 4） |
| HEAD | `469e5ef3c6d079844d32e775cd6edd7b3a9e15ad` |
| dirty | 仅未跟踪 `03审计报告/` |
| `current_state.state_revision` | 243 |
| `current_state.source_commit` | `a5ce3bf33ce16a97af83710819c30103c6a120f7`（HEAD 为其后一个治理 hash 同步提交，属已知自引用环） |
| `start-check --strict` | **通过**：PASS=13 WARN=1 FAIL=0 |
| 工作树绑定 | 本对话未绑定 `W###`；只读定位。W001/W007 代码分别只读核验其工作树 |

### 本次 start-check 原文

```
[PASS] JSON schemas validate current state, document registry and directives
[PASS] current_state required fields
[PASS] MPP repair evidence registry matches current state
[PASS] active directives have no topic conflicts
[PASS] accepted legacy result envelope gap acknowledged: mpp1_std10val_xzy_ext_uni2h_mlp_20260706
[PASS] accepted legacy result envelope gap acknowledged: mpp2_std10val_xzy_ext_uni2h_mlp_20260706
[PASS] accepted legacy result envelope gap acknowledged: mpp3_std10val_embargo_xzy_ext_uni2h_mlp_20260706
[PASS] accepted legacy result envelope gap acknowledged: mpp4_std10val_xzy_ext_uni2h_mlp_20260706
[PASS] accepted legacy result envelope gap acknowledged: mpp5_std10val_embargo_xzy_ext_uni2h_mlp_20260706
[PASS] CURRENT_STATE.md matches current_state.json
[PASS] PROJECT_GUIDE.md matches document registries
[PASS] workflow governance v3 workspace/asset/approval/attempt/science/fact/workflow state validates
[PASS] state package validation completed
[WARN] MPP standardized labels contain duplicate/conflicting barcodes; regeneration requires a separate approved task
[SUMMARY] PASS=13 WARN=1 FAIL=0
[PASS] Agent start gate passed. Read CURRENT_STATE.md before acting.
```

barcode WARN 是已知受保护旧标签债务；active 训练绑定 repaired staging，不构成本轮 FAIL。

## Sources inspected

| Source | Lifecycle | What it proves |
|---|---|---|
| 本次 git locus + `start-check --strict` | `current` | 门禁通过，允许出具 CA2 判定 |
| `project_state/current_state.json` MPP2 / accepted / blocked_actions / mpp_repair | `current` | 四实验中基线/Huber/W007 在 latest accepted；Ridge 不在；训练须绑定 repaired manifest |
| `experiments/experiment_registry.json` 四条记录 | `accepted` 或 `rejected` | 标识、选模合同、bundle、登记指标 |
| `project_state/document_registry.json` 中 `lifecycle=active` 相关项 | `current` | 执行依据：`AGENTS.md`、`configs/server_paths.yaml`、`project_state/plans/mpp_training.md`、Ridge 计划、pfmval-audit Skill |
| W007 三份分析报告 | `historical` | 对照，不替代源码与 accepted bundle |
| `dataset_mpp_manifest.py`、`train_mpp_uni2h_mlp.py`、`scripts/rebuild_zscore_from_manifest.py`、`scripts/generate_standard_splits.py`、`scripts/fit_mpp2_pathway_ridge_calibration.py` | `current` | 基线/Huber/Ridge 实现 |
| W001 `train_mpp_uni2h_mlp.py`（HEAD=`a04319a1…`，与 Huber `source_commit` 一致） | `current`（工作树源码） | Huber 损失进计算图 |
| W007 `experiments/workspaces/W007/mpp2_cpgcr_probe/` | `current`（W007 工作树源码） | 残差臂优化器、XZY 边界、复放合同 |
| `mpp_standard_splits/group_2/{split_manifest,split_info,zscore_*}` | `current` / 受保护资产只读 | 划分与 z-score 元数据 |
| `mpp_standard_splits/path_index.json` 中 group_2 旧标签 | `current`（已知冲突） | **不是** accepted 训练输入 |
| `project_state/evidence/mpp/barcode-repair-20260711-d626ad8-v003/server_asset_audit_manifest.json` | `accepted`（active repair 审计） | 修复标签 `duplicate_barcode_count=0` |
| 修复基线 / Huber / Ridge / W007 accepted 或 rejected 产物 | `accepted` 或 `rejected` | 复放、预测差、XZY 时序 |

## Checks

| Result | Check | Fresh evidence | Boundary |
|---|---|---|---|
| PASS | start-check --strict FAIL=0 | 本次输出 PASS=13 WARN=1 FAIL=0 | `current` |
| PASS | CA2-1 硬 Bug：manifest 路径无行序 fallback；修复标签无重复 barcode；split 无 patch 错配 | 见下节 | `current`+`accepted` |
| WARN | CA2-1 协议：in-repo 旧 train CSV 仍冲突；ManifestMPPDataset 无重复硬门 | path_index；`dataset_mpp_manifest.py` dict 覆盖 | 不把旧标签当训练输入 |
| PASS | CA2-2 硬 Bug：z-score 只在 train 拟合；inverse 用同一套参数 | zscore_manifest `fit_split=train`；params sha 与 Ridge provenance 一致 | `current`+`accepted` |
| WARN | CA2-2 协议：训练在 z 空间，raw 指标由同一套 mean/std 反变换 | `train_mpp_uni2h_mlp.py` inverse 注释 | 不是错用另一套参数 |
| PASS | CA2-3 硬 Bug：无同一 patch 进 train 与 val；XZY 不在划分内 | 当前 split_manifest：patch 交集 0，无 XZY；`leakage_pairs=0` | `current` |
| WARN | CA2-3 协议：六患者身份同时出现在 train 与 val | split_info `val_ratio_target=0.1`；六患者重叠 | 当时登记协议，不是硬 Bug |
| PASS | CA2-4 硬 Bug：FBR/基线/Huber-MSE 复放 val_loss/PCC | FBR `baseline_replay_check.json` status=PASS；基线与 A003 `best_val_loss=0.374886` | `accepted` |
| WARN | CA2-4 协议：选模用 internal val_loss，相对 XZY 可能偏乐观 | 内部 PCC≈0.797 vs 外部≈0.655 | 不是复放失败 |
| PASS | CA2-5 硬 Bug：Huber/残差臂进入优化器且预测可测改变 | Huber max‖Δpred‖=0.389；W007 相对 FBR 全行非零差 | `accepted`+`current` |
| WARN | CA2-5 协议：外部 PCC 变化很小 | Huber ΔPCC=-0.0022；W007 各臂 | 超参数对话，不记硬 Bug |
| PASS | CA2-6 硬 Bug：XZY 不参与选模；Ridge r002 未读 XZY；r003 先冻校准器 | 见下节 | `accepted`/`rejected`/`current` |
| WARN | CA2-6 协议：internal-val 与 train 共享患者身份 | 同 CA2-3 | 不是读 XZY |

## CA2 六项双栏表（硬 Bug | 协议）

### CA2-1 图像、通路、标签、患者 ID 逐行对应

| 栏 | 判定 | 证据 |
|---|---|---|
| 硬 Bug | **PASS** | 四实验均 `val_strategy=manifest`。`dataset_mpp_manifest.py` 用 `barcode`==`.pt` stem 1:1 匹配，**无行序 fallback**；`allow_missing=False` 时未匹配即 `ValueError`。`dataset_mpp.py` 的行序 fallback 不在本路径。当前 `split_manifest.csv`：10550 行，train 9472 / val 1078；`patient+patch_stem` 重复 0；train∩val patch 身份 0；无 XZY；stem 中 x/y 与列完全一致。active repair 审计：`group_2/labels/train/HYZ15040/...` `row_count=1309` `duplicate_barcode_count=0` sha256 `7402b5a5…ced7`（与 in-repo 冲突文件 sha `624231e3…` 不同）。W007 `_read_unique_label_stems` 遇重复即 `ContractError`，四臂仍 accepted。重建脚本对 generated label 有 duplicate hard gate。 |
| 协议 | **WARN** | in-repo `mpp_standard_splits/group_2/labels/train/{HYZ,LMZ,TGC,ZHZ}` 按 path_index 仍有冲突重复（HYZ/LMZ 各 529，TGC/ZHZ 各 314，max‖Δz‖ 约 4.8–6.1）。这是 start-check barcode WARN 所指的**旧标准化标签**，不是 barcode-repair staging。`train_mpp_uni2h_mlp.py` 强制 `--manifest_labels_root` 等于 `server_stage_path`。`ManifestMPPDataset` 对重复 stem 仍是 dict 后写覆盖（不如 `dataset_mpp.py` / W007 硬失败）。当前 git 跟踪的 split_manifest sha256=`1f7d1345…` ≠ 训练绑定 `babfa2b1…`（repair 源与 W001 job 均为后者）；本轮唯一性核验打在当前文件上，逻辑计数与 split_info 一致。 |

### CA2-2 z-score 只在训练折拟合

| 栏 | 判定 | 证据 |
|---|---|---|
| 硬 Bug | **PASS** | `group_2/zscore_manifest.json`：`fit_split=train`，`fit_patients` 仅六训练患者，`excluded_splits` 含 internal_val / external_test / embargo；`n_train_samples=9472`。`zscore_params_from_train.json` 同结构；文件 sha256 `fd65f6ab3a2dda2d8d9f8f17babe59818ea7d93f2480c5da2bcd6384ca37b2c8` 与 Ridge r003 `model_provenance.json` 一致。`rebuild_zscore_from_manifest.py` 只在 `split=="train"` 上 `nanmean/nanstd(ddof=1)`，再应用到 val/XZY。`train_mpp_uni2h_mlp.py` preflight：`external_patient` 不得出现在 `fit_patients`；inverse-z 读同一 `zscore_params_from_train.json`（`pred_raw = pred_z * std + mean`）。W007 同样拒绝 XZY 进入 fit_patients。 |
| 协议 | **WARN** | 优化目标在 train-z 空间；raw MAE/R² 由同一套参数反变换。R² 对仿射不变，故 z/raw R² 同值。这不是 val/XZY 参与拟合。 |

### CA2-3 同一患者全部切片是否进入同一集合

| 栏 | 判定 | 证据 |
|---|---|---|
| 硬 Bug | **PASS** | 当时登记协议是 **六患者内空间约 10% val**（`split_info.json`：`val_ratio_target=0.1`，各患者 val_ratio 0.0997–0.1061；`leakage_pairs=0`）。硬 Bug 定义是违反该协议的泄漏。当前 manifest：同一 `patient+patch_stem` 不跨 split；train/val **patch 身份交集 0**；XZY 不在 manifest。W007 对 train/val 身份交集硬失败。`generate_standard_splits.py` 按患者 block 抽样，并声明同一 patch_stem 不得跨 split。 |
| 协议 | **WARN** | 六名训练患者身份同时出现在 train 与 val。这是现行登记协议，不是硬 Bug。划分子对话可讨论是否改为患者级 holdout。 |

### CA2-4 baseline checkpoint 精确复放

| 栏 | 判定 | 证据 |
|---|---|---|
| 硬 Bug | **PASS** | 修复基线 accepted：`best_epoch=15`，`best_val_loss=0.374886`，`best_val_pcc=0.7971`，`external_xzy_pcc=0.6549`（`automation/results/mpp2-repair-v003-frozen-20260711-r002/attempt-003/`）。W007 FBR `baseline_replay_check.json`：`status=PASS`，observed val_loss=`0.37488633394241333`（Δ=3.34e-7），observed val_pcc=`0.7971258175957328`（Δ=1.82e-7）；`checkpoint.json` 标明 historical accepted checkpoint，未重选。Huber MSE 对照 A003 独立再训：`best_val_loss=0.374886`，外部 PCC=0.6549，与基线一致。Ridge r003 内部基线 `mean_z_mse=0.3748863316`、`legacy_pooled_pcc=0.7971258175`，落在登记 1e-5 容差内。 |
| 协议 | **WARN** | 选模信号是 internal val_loss；内部 PCC≈0.797、外部≈0.655。复放通过不能推出选模指标对外部不乐观。 |

### CA2-5 新增参数进入优化器并真实改变预测

| 栏 | 判定 | 证据 |
|---|---|---|
| 硬 Bug | **PASS** | Huber：`--loss huber --huber-delta 1.0` → `nn.HuberLoss`；`Adam(model.parameters())`。A003 vs A004 外部预测 1039×30：max‖Δ‖=0.389，mean‖Δ‖=0.0278，**100% 元素非零**；登记 PCC 0.654896 vs 0.652691。W007：`optimizer_parameters()` 只含 `image_projector`/`residual_head`（及 HCR/CPGCR 的 `pathway_encoder`）；B0 `requires_grad=False` 且不得进 optimizer。RCC 相对 FBR 外部预测 max‖Δ‖=0.352、mean 0.045，1039 行 key 对齐；HCR max 0.103；CPGCR max 0.270。HCR `train_contrastive_loss` 从 1.25 降到 ~0.29；CPGCR 对比损失约 3.33（非死图）。RCC 对比损失恒 0 是该臂无通路编码器的设计，残差仍改变了预测。 |
| 协议 | **WARN** | 外部 pooled PCC 变化很小（Huber -0.0022；W007 各臂与 FBR 同在 0.6538–0.6560）。有可测差异但幅度小，不记硬 Bug。 |

### CA2-6 external 未参与选模、阈值、超参

| 栏 | 判定 | 证据 |
|---|---|---|
| 硬 Bug | **PASS** | 基线/Huber：`early_stop_signal=val_loss`；history 无 XZY 列；先 load best_state 再 `evaluate(external_loader)`。A003/A004 `metrics.json`：`external_xzy_role=evaluated_once_after_checkpoint_frozen` / `XZY did not select checkpoint or tune parameters`。W007：`DataAccessBoundary("internal_only")` 在训练期禁止解析 XZY；`external_may_select_checkpoint` 必须为 false；`timing=once_after_each_internal_checkpoint_freeze`。Ridge **r002** 产物只有 `nested_lopo_metrics.json` 与 `pathway_decisions.csv`，无 `calibrator.json`、无 `predictions_external_xzy.csv`；nested 文本不含 XZY；与 Registry「XZY was not read」及代码在 internal gate 失败时 `raise` 且不进入 Stage 3 一致。Ridge **r003**：`calibrator.json` 中 `final_lambda=10.0` 来自六折 internal `fold_scores`；`model_provenance.calibrator_sha256` 写入后才有 `predictions_external_xzy.csv`；失败原因是 `external_gate.passed=false`（raw R²/MAE 未过门），**没有**用 XZY 重选 λ。 |
| 协议 | **WARN** | 合同未读 XZY 选模，但 internal-val 与 train 共享患者身份（同 CA2-3）。 |

## Verdict

- Overall: `CONDITIONAL GO`
- Reason: 六项**硬 Bug 全 PASS**。可以停止把 PCC≈0.65 归因于一般性硬 Bug（错配、z-score 泄漏、违反当时协议的切片泄漏、无法复放、新头未训练、用 XZY 选模）。仍点名协议/资产条件，因此不是无条件 `GO`。不对划分改进或新训练给 GO。
- Evidence scope: 本机 2026-08-21 HEAD `469e5ef`；源码 + Registry accepted/rejected 产物 + repair 审计。未重训、未重建 z-score/划分。本机无 repaired staging 目录（`D:\AIPatho\Patch\mpp_zscore_repair_staging\...` 不存在），修复标签唯一性以 canonical audit manifest 为准。

点名条件（非硬 Bug，不授权修代码对话）：

1. 现行协议本就是六患者内空间 10% val；患者身份重叠交给划分/z-score 诊断对话。
2. 仓库内旧 `mpp_standard_splits/group_2/labels/train` 仍有冲突重复 barcode；accepted 运行绑定的是 `barcode-repair-20260711-d626ad8-v003`（审计 `duplicate_barcode_count=0`）。不得把旧 CSV 当成训练标签。
3. 当前跟踪的 `split_manifest.csv` sha256 `1f7d1345…` 与训练绑定 `babfa2b1…` 字节不同；当前文件无 patch 跨 split，但不是 Huber job 的逐字节同一文件。

## Remaining

- Blocking: 无 CA2 硬 Bug。
- Non-blocking: 上列三条条件；barcode WARN；`03审计报告/` 未跟踪；Ridge 计划仍 active 但 experiment 已 rejected。
- Next authorized action: `停止怀疑一般性硬 Bug，允许开启划分与 z-score 诊断对话`（仍须用户勾选接受本回传；本对话不开诊断、不写部署方案）。

## 若需修复

无必须先修的代码点。不提供补丁。

可选后续（**不是**本对话授权，也不是硬 Bug 修复面）：若未来有人误用 in-repo 旧 train CSV，`ManifestMPPDataset` 的重复覆盖会比 W007/rebuild 更弱——属稳健性，交治理/划分对话，不在本 CA2 修。

## 给总管的下一允许动作

`停止怀疑一般性硬 Bug，允许开启划分与 z-score 诊断对话`

## 明确未做

未修代码、未训练、未派发、未 import/accept、未改 Registry、未重做划分或 z-score、未写部署方案、未创建实施方案、未 commit、未 push、未 `git add 03审计报告/`、未 state sync、未评价超参数、未解释 PCC 0.65 科学上限。上一轮 `NOT APPLICABLE` 稿未当作本轮结论。
