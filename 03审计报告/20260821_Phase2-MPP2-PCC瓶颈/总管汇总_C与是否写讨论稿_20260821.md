# 总管汇总：C（划分与 z-score）及是否写讨论稿

- 日期：2026-08-21
- 角色：总管汇审，不是查找复跑，不是修复
- 本文件默认 `pending_review`，不登记为 `document_registry` 的 `active` 结论，不授权训练

## Locus（本次总管新鲜核验）

| 项 | 值 |
|---|---|
| branch | `main`（`origin/main` ahead 4） |
| HEAD | `469e5ef3c6d079844d32e775cd6edd7b3a9e15ad` |
| Git-visible dirty | 仅未跟踪 `03审计报告/`；另有被 `.gitignore` 隐藏的本轮讨论稿，不出现在普通 `git status` |
| `start-check --strict` | PASS=13 WARN=1 FAIL=0（barcode WARN） |

与对话 C 报告中的 locus **同一 HEAD**，无新的治理提交需要另行标明。

## 可检验命题

1. C 的 `CONDITIONAL GO`（支持写划分/z-score 讨论稿，不是训练 GO）是否成立？
2. 现在是否修代码、重训、定版绑定 W008，或只写非定版讨论稿？

## 证据分类

| 材料 | 类 |
|---|---|
| Registry 冻结基线 / Huber / W007 | `accepted` |
| Ridge r003 | `rejected` |
| C 正式报告与审批回传 | `pending_review`（查找产物） |
| 用户于 2026-08-21 声明「接收判定」 | 人工观察：用户已接受该 `CONDITIONAL GO`。**不**升格为 registry `active` |
| A/B 总管汇总 | `pending_review` |
| 本轮 start-check 与 `zscore_manifest.json` / `split_info.json` / Registry 抽查 | `current` / `accepted` |
| `独立复核补充_训练绑定清单与Git留档_20260821.md` | `pending_review` | 限定历史训练 manifest 的证据强度，并暴露文档未跟踪状态 |

## 抽查（未重做逐通路诊断）

1. **locus / start-check**：C 记 HEAD `469e5ef`、FAIL=0；本轮相同。可复述为当前仓库事实。
2. **硬门 z-score**：`group_2/zscore_manifest.json` 现为 `fit_split=train`，`excluded_splits` 含 `internal_val` / `external_test` / `embargo`；六人在 `fit_patients`。未发现 val/XZY 拟合。
3. **硬门划分**：`split_info.json` 现为 `val_ratio_target=0.1`，`fit_patients` 为同一六人。
4. **Registry 冻结基线**：`best_val_pcc=0.7971`，`external_xzy_pcc=0.6549`，`external_xzy_r2_raw=-0.088`。与 C 引用一致。
5. **事实 / 假设 / 不得执行**：C 分节清楚。oracle 均值对齐标为「使用了 XZY 标签，禁止当方法」；V3bis 标 `historical`，禁止锁定协议。
6. **超参对照**：δ=1、λ=10、W007 解耦只作对照；C 未用 XZY 反推新值，未重生成划分/z-score。
7. **未发现**：把假设写成可执行结论、用 XZY 选方案、或 val/XZY 拟合 z-score。

独立复核补充后，仍不回到 CA2 修代码，也不因 C 而 `NO-GO` 讨论稿；但“六项硬 Bug 全 PASS”须按以下边界理解：当前源码、accepted 结果和可用资产未显示一般性硬 Bug，训练绑定 split 的逐行内容尚未在本机恢复，因此 CA2-1 / CA2-3 对实际历史运行是**条件性通过**，不是完整证据闭合。

点名条件（C 已写，总管保留并补充）：当前 `split_manifest.csv` sha `1f7d1345…` ≠ 训练绑定 `babfa2b1…`；in-repo 旧 train CSV 不得当训练输入；不能把 0.80−0.65 写成已定量百分比；“每名患者 1 张物理捕获切片”只能写成 manifest 层观察，等待采集元数据确认。

## 总管判定

| 决策 | 判定 |
|---|---|
| C 的 `CONDITIONAL GO` 是否成立 | `CONDITIONAL GO`（成立，带上列点名条件） |
| 现在修 MPP2 训练代码 | `NO-GO` |
| 现在开 Huber / W007 / LoRA / Ridge 重训或改超参合同 | `NO-GO` |
| 现在写定版部署方案并绑定 W008 / 写实施方案 | `NO-GO` |
| 现在在 `01_指南与解读/部署方案/` 写一份划分/z-score **讨论稿（非定版）** | `GO` |

## 下一步

总管撰写讨论稿：`01_指南与解读/部署方案/MPP2划分与zscore协议_讨论稿_20260821.md`。  
用户于 2026-08-21 已接受 ED1 有边界结论、GD2 精确 Git 留档和 PD3-1 六折患者级 LOPO 方向，并明确否决旧 PF1–ET6 的嵌套选模/延后外测设计。当前讨论稿采用 BF1–RP6：只检验与 accepted repaired MPP2 baseline 相同的模型合同，唯一变化是五患者训练/一完整患者 internal-val 的六折划分及 fold-specific train-only z-score；每折 internal-val 用于调优和 early stopping，checkpoint 冻结后立即 XZY 外测。本轮固定为 6 次训练 + 6 次 XZY eval，不比较候选模型、不挑单个 fold checkpoint、不额外训练全六患者 final model。讨论稿仍为 `pending_review`；BF1–RP6 待用户整体审核。批准最终方案并确认准备绑定实验之前，不得写实施方案、不得绑定 W###。
