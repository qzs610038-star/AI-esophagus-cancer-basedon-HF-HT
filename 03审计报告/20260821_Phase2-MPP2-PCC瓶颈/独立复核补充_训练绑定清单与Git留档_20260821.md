# PFMval independent audit：训练绑定清单与 Git 留档补充

- 日期：2026-08-21
- lifecycle：`pending_review`
- 角色：对 CA2 R2、划分/z-score 诊断、总管汇总和讨论稿的独立补充复核
- 边界：不覆盖原报告；本文件对其中证据强度过高或未写明的部分作限定
- 用户决定（2026-08-21）：ED1 接受推荐项；GD2 接受精确 Git 留档。该接受不等于批准最终划分协议、实施方案或训练。

## Claim

- Testable claim：现有证据是否足以把 MPP2 六类一般性硬 Bug 写成“全部完整闭合”，以及新建审计材料和讨论稿是否具备可审阅、可留档状态。
- Requested decision：用户是否接受有边界的 `CONDITIONAL GO`，以及是否授权对指定文件做精确 Git 留档。
- Scope and identifiers：
  - 当前仓库：`main` / HEAD `469e5ef3c6d079844d32e775cd6edd7b3a9e15ad`
  - repaired baseline：`mpp2_barcode_repair_v003_frozen_baseline_20260711`
  - Huber：`mpp2_huber_loss_paired_v001_20260728`
  - W007：`mpp2_cpgcr_probe_v001_20260811`
  - 训练绑定 data manifest：`barcode-repair-20260711-d626ad8-v003:1204018178a4d355`

## Sources inspected

| Source | Lifecycle | What it proves |
|---|---|---|
| `project_state/current_state.json` 相关字段 | `current` | active repaired manifest、阻断动作和当前 revision |
| `experiments/experiment_registry.json` 四项相关实验 | `accepted` / `rejected` | baseline、Huber、Ridge、W007 的身份、状态和登记指标 |
| `server_asset_audit_manifest.json`、W001 job/contract | `accepted` / `current` | 训练绑定的 split manifest 身份记录为 `babfa2b1…` |
| 当前 `group_2/split_manifest.csv`、`split_info.json` | `current` | 当前清单的结构、计数和患者内空间划分；不自动证明历史训练绑定文件内容 |
| CA2 R2 与划分/z-score 诊断 | `pending_review` | 已做的源码、结果和分布核验及其自报边界 |
| `.gitignore`、`git status --short --untracked-files=all` | `current` | 讨论稿被忽略、审计目录未跟踪 |

## Checks

| Result | Check | Fresh evidence | Boundary |
|---|---|---|---|
| PASS | 当前门禁允许出具文档结论 | `start-check --strict`：PASS=13 WARN=1 FAIL=0 | barcode WARN 仍有效 |
| PASS | Registry 指标与 Cursor 引用一致 | internal PCC=`0.7971`、external PCC=`0.6549`、raw R²=`-0.088` | 仅限 accepted baseline 声明范围 |
| PASS | train-only z-score、XZY 不参与选模的主结论有源码和结果支持 | fit_split=train；checkpoint 冻结后评估 XZY | 不等于新患者泛化无偏 |
| WARN | 历史训练绑定 split 的逐行内容未在本机闭合 | 当前文件记录身份 `1f7d1345…`，训练绑定记录为 `babfa2b1…`；baseline/Huber source commit 与 HEAD 中该路径均为同一 Git blob，但不是训练绑定文件 | 未重新计算任何哈希；这里只使用已有记录和 Git 对象身份 |
| WARN | CA2-1/CA2-3 对“实际历史训练清单”的证据强度写得过高 | patch 交集、逐行身份和患者集合核验打在当前清单；不能自动外推到字节不同的训练绑定清单 | 当前实现/当前清单仍未发现一般性硬 Bug |
| WARN | “每名患者对应 1 张物理捕获切片”缺少采集元数据闭环 | manifest 只显示每个 patient 一套坐标且没有 `slice_id` 字段 | 可写成 manifest 层观察，不能当物理切片事实 |
| WARN | 新建文档尚未可靠留档 | `03审计报告/` 全部未跟踪；`01_指南与解读/部署方案/*` 被 `.gitignore` 覆盖 | 普通 `git status` 看不到讨论稿，普通 `git add` 不会纳入它 |

## 对原结论的限定

1. 保留总体 `CONDITIONAL GO`：当前无需因为现有证据立即修 MPP2 训练代码，也不授权重训。
2. 将“六项硬 Bug 全 PASS、已经全部排除”收紧为：
   
   > 当前源码、accepted 结果和可用资产没有显示一般性硬 Bug；但训练绑定 split 的逐行内容未在本机恢复，因此 CA2-1（逐行对应）和 CA2-3（实际历史 split 身份）对该次历史运行属于条件性通过，而不是完整证据闭合。
3. 将“每名患者对应 1 张 Visium HD 捕获切片”收紧为：
   
   > 当前 manifest 对每名患者仅暴露一套坐标且没有第二切片字段；是否确为一人一张物理捕获切片，仍需采集元数据确认。
4. 当前证据足以支持写讨论稿，因为讨论稿不授权训练，也不依赖先证明历史清单逐字节相同。

## Verdict

- Overall：`CONDITIONAL GO`
- Reason：Cursor 对指标、train-only z-score、患者内空间验证和 XZY 边界的主判断成立；历史训练绑定清单的逐行闭环与文档版本留档仍待处理。
- Evidence scope：本机当前源码、机器状态、accepted/rejected Registry 记录、现有结果包及当前清单。没有服务器现场核验，没有恢复或重生成受保护 manifest。

## Remaining

- Blocking：对“保留非定版讨论稿”无阻断；对“宣布历史训练六项全部证据闭合”仍有阻断。
- Non-blocking：物理切片数需要采集元数据才能升级为事实；barcode WARN 保留。
- Next authorized action：按 GD2 对本审计目录和指定讨论稿形成精确本地 Git 留档；不取回受保护资产、不写实施方案、不绑定 W###。

## 待用户决定

### ED1：证据闭环决策（Evidence Decision 1）

- **已接受推荐项（2026-08-21）：接受有边界结论。** 不为当前讨论稿专门取回历史训练 manifest；保留 CA2-1/CA2-3 的条件性措辞。理由是当前不训练，取回只提高历史证明强度，不会决定患者留出或 LOPO 哪个更好。
- 或：要求经现行 Gitee 治理通道取回训练绑定的精确 manifest，再做逐行核验。该路线涉及受保护资产与传输治理，需另行明确授权，不能用服务器直连替代。

### GD2：Git 留档决策（Git Documentation Decision 2）

- **已接受推荐项（2026-08-21）：精确留档。** 仅纳入本审计目录和这一份讨论稿；不放开整个部署方案目录，不推送 remote。
- 或：继续只保留本机未跟踪/被忽略文件，接受其不会随常规提交保存的风险。

ED1/GD2 已完成决策。下一项才是 PD3：协议方向决策（Protocol Direction Decision 3）；在 PD3 前仍不需要 W008 或训练参数。
