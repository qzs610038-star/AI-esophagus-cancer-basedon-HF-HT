# 审批回传模板（查找对话必填）

查找对话结束时，把本模板复制为 `审批回传_<任务名>_YYYYMMDD.md`，填入后写入同一主题目录，并在聊天中用同样结构做短回传。

---

## 回传头部

- 任务：CA2 代码审计 / 超参数审查 / 划分与 z-score 诊断
- 对话角色：查找，不是总管，不是修复
- 仓库 locus：branch / HEAD / dirty
- `start-check --strict`：通过 / 失败（失败则下面结论全部作废）
- 正式报告路径：
- 事实截止时间：

## 可检验命题与判定

- 命题：
- 总体判定：`GO` / `CONDITIONAL GO` / `NO-GO` / `NOT APPLICABLE`
- 判定覆盖范围（一句话，写明没有覆盖什么）：

## 分项结果

| 项 | 硬 Bug | 协议/设计 | 证据类 | 证据位置 |
|---|---|---|---|---|
|  | PASS/WARN/FAIL | PASS/WARN/FAIL/不适用 | current/accepted/… |  |

## 给总管的下一允许动作（只选一个）

- [ ] 等待用户审核本回传
- [ ] 授权另开修复代码对话（仅当硬 Bug FAIL）
- [ ] 停止怀疑一般性硬 Bug，允许开启划分与 z-score 诊断对话
- [ ] 超参数审查完成，不授权训练；交总管决定是否纳入后续合同
- [ ] 划分/z-score 诊断支持总管去 `01_指南与解读/部署方案/` 写讨论稿（仍不绑定工作树）
- [ ] 证据不足，需要用户补充材料后再审

## 请用户当场批准

- [ ] 接受本回传的总体判定
- [ ] 接受“下一允许动作”
- [ ] 不接受；需要返工的点：

## 明确未做

列出本对话没有做的事，例如：未修代码、未训练、未 import、未重生成 z-score、未写部署方案、未 commit。
