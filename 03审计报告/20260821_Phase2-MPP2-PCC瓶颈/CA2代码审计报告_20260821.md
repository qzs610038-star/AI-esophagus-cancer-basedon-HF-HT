# PFMval independent audit

- 任务：CA2 代码审计（只读）
- 对话角色：查找，不是总管，不是修复
- 报告日期：2026-08-21
- 本报告性质：preflight 阻塞记录。未出具 CA2 六项硬 Bug 判定，也未授权停止怀疑一般性硬 Bug。

## Claim

- Testable claim: 在 MPP2 修复基线及其后 Huber / Ridge / W007 实现中，CA2 规定的六类一般性硬 Bug 是否存在。若全不存在，可停止继续怀疑一般性硬 Bug。
- Requested decision: 对“可否停止怀疑一般性硬 Bug”给出 `GO` / `CONDITIONAL GO` / `NO-GO`；不覆盖划分改进、新训练或 PCC 0.65 的科学上限。
- Scope and identifiers:
  - `mpp2_barcode_repair_v003_frozen_baseline_20260711`（accepted；result_id `mpp2-repair-v003-frozen-20260711-r002-result-20260711164140-f2b1b51d`）
  - `mpp2_huber_loss_paired_v001_20260728`（W001；accepted）
  - `mpp2_pathway_ridge_calibration_v001_20260717`（status=`failed`，evidence=`rejected`；仍须审 r002/r003 是否在冻结校准器前读 XZY）
  - `mpp2_cpgcr_probe_v001_20260811`（W007；accepted；FBR/RCC/HCR/CPGCR）
  - 数据清单：`barcode-repair-20260711-d626ad8-v003:1204018178a4d355`
  - LoRA：不在本次 CA2 主审

## Locus

| 项 | 值 |
|---|---|
| branch | `main`（跟踪 `origin/main`，ahead 2） |
| HEAD | `f02d528c49dad95b3bbc3745e553c26c87723f47` |
| dirty | 仅未跟踪 `03审计报告/`（本审计主题目录） |
| `current_state.source_commit` | `dbb196ba205127cf8361d5bd5bf458cfcb5cdf78`（落后 HEAD 2 个治理提交） |
| `current_state.state_revision` | 242 |
| `start-check --strict` | **失败**（PASS=11 WARN=1 FAIL=2，BLOCKED） |
| 工作树绑定 | 本对话未绑定 `W###`；只读定位 |

HEAD 相对 `current_state.source_commit` 的两个提交：

- `05f86ac` governance: 精简项目 Skill 目录，仅保留 4 个权威技能及 Claude 适配器
- `f02d528` governance: drop skill and AGENTS.md content-hash gates from start-check

## Sources inspected

| Source | Lifecycle | What it proves |
|---|---|---|
| 本次 `git status --short --branch` / `git rev-parse HEAD` | `current` | 仓库 locus；未跟踪本主题目录 |
| 本次 `python deploy/pfmval_ops.py agent start-check --strict` | `current` | 结论门禁未通过；不得出具 CA2 GO/NO-GO |
| `project_state/current_state.json` 中 MPP2 / accepted / blocked_actions / mpp_repair 字段 | `current` | 四个目标中 Huber 与 W007、修复基线在 `latest_accepted_result_ids`；Ridge 不在该列表；blocked_actions 含不得无批准训练、不得隐式再生 MPP 资产、不得把 pending 当当前结论 |
| `experiments/experiment_registry.json` 四条 experiment 记录 | `accepted`（基线/Huber/W007）或 `rejected`（Ridge） | 标识、选模合同、bundle 路径与登记指标；**不**证明本次已核验源码硬 Bug |
| `project_state/document_registry.json` 中 `lifecycle=active` 相关记录 | `current` | 执行依据仅限 active；W007 原因分析三份为 `historical`，不得当当前结论 |
| `.agents/skills/pfmval-audit/SKILL.md` 与 evidence-boundaries | `current`（Skill 正文；registry 中 SKILL.md 为 active/normative） | 审计工作流与证据等级 |
| `git worktree list --porcelain` | `current` / `diagnostic_only` | FAIL-2 的直接原因：存在 prunable、路径已不存在的临时 worktree |
| `scripts/pfmval_state.py` 中 `compute_source_hashes` / workspace `resolve(strict=True)` | `current` | 解释两项 FAIL 的检查机制；不授权修复 |
| W007 分析报告三份、总管聊天、待审归因 | `historical` 或未采用 | 对照线索，不替代源码与 accepted bundle |

与本任务相关且 `lifecycle=active` 的执行依据（不全表载入）：

- 规范性：`AGENTS.md`、`configs/server_paths.yaml`、`project_state/plans/mpp_training.md`、`project_state/plans/mpp2_pathway_ridge_calibration_v001_20260717.md`、`.agents/skills/pfmval-audit/SKILL.md`
- 参考但仍为 active：`01_指南与解读/分析报告/MPP2后续方案与LoRA新数据实验建议_20260709.md`（不得覆盖 Registry 中 accepted 结果）
- 明确不得当当前结论：`01_指南与解读/分析报告/W007_四臂结果核验与待审结论_20260815.md`、`W007指标未改善原因分析报告_20260815.md`、`W007指标未改善深度归因与机制解读报告_20260815.md`（均为 `lifecycle=historical`）

## Checks

| Result | Check | Fresh evidence | Boundary |
|---|---|---|---|
| FAIL | 出具 CA2 结论前 `start-check --strict` 必须通过 | 本次命令输出：`[SUMMARY] PASS=11 WARN=1 FAIL=2` / `[BLOCKED] Resolve critical state conflicts before continuing.` | `current`；失败则停止写 CA2 结论 |
| FAIL | `document_registry_sha256` 与 `current_state.source_hashes` 一致 | expected `41a4af03480cda644b8c610fd2361f2fdeb66cefc1552bcc10db1cc362638da3`；actual `0571549ad3846de63207eeff428db45f5fed62b90eee495e24240eea1f63137b`；其余 hard hash 均匹配 | `current`；HEAD 两提交改写了 `document_registry.json`，hash 未回写 current_state |
| FAIL | workflow governance v3 工作树路径可解析 | `git worktree list` 含 `C:/Users/33696/AppData/Local/Temp/pfmval-w004-governance-f8a1c2f`，标注 `prunable gitdir file points to non-existent location`；`Test-Path` = False；校验在 `Path.resolve(strict=True)` 处抛出 `[WinError 2]` | `current`；与 MPP2 训练源码无直接关系，但仍阻塞 strict 门禁 |
| WARN | MPP 标准标签存在重复/冲突 barcode | start-check：`[WARN] MPP standardized labels contain duplicate/conflicting barcodes; regeneration requires a separate approved task` | `current`；已知保护资产告警，不构成本次 FAIL，也不得在本对话重生成 |
| PASS | 本次 locus 命令可复述 | `main...origin/main [ahead 2]`；HEAD `f02d528c...`；dirty 仅 `?? 03审计报告/` | `current` |
| 未出具 | CA2-1 图像/通路/标签/患者 ID 逐行对应 | 未打开训练数据集与 accepted 预测做行级核验 | 因 preflight 停止 |
| 未出具 | CA2-2 z-score 仅在训练折拟合 | 未核验重建脚本与 group_2 参数加载 | 因 preflight 停止 |
| 未出具 | CA2-3 切片泄漏是否违反当时登记协议 | 未核验 split_info 对当时协议 | 因 preflight 停止 |
| 未出具 | CA2-4 FBR / 修复基线精确复放 | 未打开 `baseline_replay_check.json` 与 accepted bundle | 因 preflight 停止 |
| 未出具 | CA2-5 新参数进入优化器并改变预测 | 未比较 Huber / W007 各臂预测文件 | 因 preflight 停止 |
| 未出具 | CA2-6 external 未参与选模；Ridge r002/r003 冻结前是否读 XZY | 未读训练脚本选模路径与 Ridge 产物 | 因 preflight 停止 |

### 本次 start-check 原文摘要

```
[PASS] JSON schemas validate current state, document registry and directives
[PASS] current_state required fields
[PASS] MPP repair evidence registry matches current state
[PASS] active directives have no topic conflicts
[PASS] accepted legacy result envelope gap acknowledged: mpp1_std10val_xzy_ext_uni2h_mlp_20260706
[PASS] accepted legacy result envelope gap acknowledged: mpp2_std10val_xzy_ext_uni2h_mlp_20260706
[PASS] accepted legacy result envelope gap acknowledged: mpp3_std10val_embargo_xzy_ext_uni2h_mlp_20260706
[PASS] accepted legacy result envelope gap acknowledged: mpp4_std10val_xzy_ext_uni2h_mlp_20260706
[PASS] accepted legacy result envelope gap acknowledged: mpp5_std10val_embargo_xzy_ext_uni2h_mlp_20260706
[PASS] CURRENT_STATE.md matches current_state.json
[PASS] PROJECT_GUIDE.md matches document registries
[WARN] MPP standardized labels contain duplicate/conflicting barcodes; regeneration requires a separate approved task
[FAIL] state source hash mismatch: document_registry_sha256
[FAIL] workflow governance v3 state invalid: [WinError 2] 系统找不到指定的文件。: 'C:\\Users\\33696\\AppData\\Local\\Temp\\pfmval-w004-governance-f8a1c2f'
[SUMMARY] PASS=11 WARN=1 FAIL=2
[BLOCKED] Resolve critical state conflicts before continuing.
```

两项 FAIL 的审计定性（不是修复授权）：

1. **hash 漂移**：`document_registry_sha256` 是 HARD key（不在 `SOFT_SOURCE_HASH_KEYS`）。差异来自 HEAD 相对 `source_commit` 的两个治理提交对 `project_state/document_registry.json` 的改写（stat：该文件 434 行变动），而 `current_state.source_hashes.document_registry_sha256` 仍为 revision 242 记录值。这是状态包自洽问题，不是 MPP2 对齐/泄漏/选模证据。
2. **失效 worktree**：主仓 `git worktree list` 仍登记已删除的临时路径 `...\Temp\pfmval-w004-governance-f8a1c2f`（HEAD `f8a1c2f494f86e997343384e7316e8e39d1096eb`，detached，prunable）。`scripts/pfmval_state.py` 在比较绝对工作树时对 `git worktree list` 每条路径 `resolve(strict=True)`，缺失即 FileNotFoundError，被包装为 `workflow governance v3 state invalid`。登记的 W004 持久路径 `D:\AI空间转录病理研究\PFMval_new_governed_workspaces\W004` 在 list 中存在且 HEAD 与 Registry `b898c9d...` 一致；阻塞点是这条 **额外的、已不存在的 Temp 条目**，不是 W004 实验源码本身。

本对话按任务书 **不得** `git worktree prune`、不得 `sync_state`、不得改 Registry。

## CA2 六项双栏表（硬 Bug | 协议）

因 start-check 失败，六项 **全部未出具**。下表不是 PASS，也不是硬 Bug FAIL。

| 项 | 硬 Bug（FAIL 则必须先修代码） | 协议/科学设计 | 新鲜证据路径与哈希/指标 |
|---|---|---|---|
| CA2-1 图像、通路、标签、患者 ID 是否逐行对应 | 未出具 | 未出具 | missing（preflight 停止） |
| CA2-2 z-score 是否只在训练折拟合 | 未出具 | 未出具 | missing（preflight 停止） |
| CA2-3 同一患者全部切片是否进入同一集合 | 未出具 | 未出具 | missing（preflight 停止） |
| CA2-4 baseline checkpoint 能否精确复放 | 未出具 | 未出具 | missing（preflight 停止） |
| CA2-5 新增参数是否进入优化器并真实改变预测 | 未出具 | 未出具 | missing（preflight 停止） |
| CA2-6 external 是否完全未参与选模、阈值、超参数 | 未出具 | 未出具 | missing（preflight 停止） |

登记事实（仅标识，**不是** CA2 核验通过）：

- 修复基线 accepted：`external_xzy_pcc=0.6549`，`best_val_loss=0.374886`，`best_val_pcc=0.7971`，`best_epoch=15`，`result_manifest_sha256=dadd0c8322b05ed626faf00bae3743550d8d226891a00e05a63f7f84a487ce4f`
- Huber accepted：control 0.6548958191047396 vs treatment 0.6526906629745176
- Ridge rejected；r002 记录写明 “XZY was not read”，r003 仍待源码与产物核验
- W007 FBR accepted 登记 external pooled_pcc=0.6548958201828085，internal pooled_pcc=0.7971258175957328（与修复基线 val 数字同量级，**未**在本轮打开 replay JSON）

## Verdict

- Overall: `NOT APPLICABLE`
- Reason: 提示词与 audit Skill 要求出具任何 PASS/FAIL/GO/NO-GO 之前必须有本次新鲜且通过的 `start-check --strict`。本次 FAIL=2，已 BLOCKED。因此 **不能** 对“停止怀疑一般性硬 Bug”给 `GO` / `CONDITIONAL GO` / `NO-GO`，也 **不能** 把六项写成 PASS 或硬 Bug FAIL。
- Evidence scope: 仅覆盖 2026-08-21 本机、本 HEAD 上的 locus 与 start-check。不覆盖 dataset 行对齐、z-score 拟合范围、split 泄漏、FBR 复放、Huber/W007 预测差、Ridge/XZY 选参。两项 FAIL 本身不构成 MPP2 一般性硬 Bug 证据，也不得被解读为“硬 Bug 已清零”。

## Remaining

- Blocking:
  1. 使 `start-check --strict` 通过，或由用户在本主题下 **显式书面授权** 本只读 CA2 对话在上述两项与 MPP2 训练代码无关的 FAIL 仍存在时继续核验并出具判定。
  2. 通过路径候选（须另开治理/维护对话，**本 CA2 对话不做**）：清理已不存在的 Temp worktree 登记；把 `document_registry_sha256` 与 current_state 对齐并更新 views。不得在 CA2 对话中 `git clean`、删除 worktree、写 Registry 或 commit。
- Non-blocking: barcode 重复 WARN；`03审计报告/` 未跟踪；Ridge 计划仍标 active 但 experiment 已 failed/rejected（后续核验时按 rejected 证据处理）。
- Next authorized action: `等待用户审核` 本阻塞回传；在用户勾选前 **不得** 开启划分与 z-score 诊断，**不得** 授权修复代码对话（因为尚未出具硬 Bug FAIL）。

## 若需修复

无 CA2 代码修复面。当前阻塞不在 `dataset_mpp.py` / 训练脚本 / W007 实验代码。精确缺陷（治理层，供用户决定是否另开维护对话）：

- `project_state/current_state.json` → `source_hashes.document_registry_sha256` 与仓库实际语义 hash 不一致
- 主仓 git worktree 元数据中的失效路径 `C:\Users\33696\AppData\Local\Temp\pfmval-w004-governance-f8a1c2f`

不含补丁实现。

## 给总管的下一允许动作

`等待用户审核`

用户审核后可选：

- 批准另开治理对话处理 start-check 两项 FAIL，通过后再开/续 CA2
- 或书面授权本对话无视这两项 FAIL、继续只读 CA2 并出具六项判定

在此之前：不允许开启划分与 z-score 诊断对话；不允许授权修复 MPP2 代码。

## 明确未做

未修代码、未训练、未派发、未 import/accept、未改 Registry、未重做划分或 z-score、未写部署方案、未创建实施方案、未 commit、未 push、未 prune worktree、未 sync_state、未打开 CA2 建议核验面的训练脚本与 accepted 预测文件作硬 Bug 判定。
