# 审批回传：划分与 z-score 诊断

## 回传头部

- 任务：划分与 z-score 诊断
- 对话角色：查找，不是总管，不是修复
- 仓库 locus：`main` / HEAD `469e5ef3c6d079844d32e775cd6edd7b3a9e15ad` / dirty=`?? 03审计报告/`（ahead origin/main 4）
- `start-check --strict`：通过（PASS=13 WARN=1 FAIL=0；仅 barcode WARN）
- 正式报告路径：`03审计报告/20260821_Phase2-MPP2-PCC瓶颈/划分与zscore诊断报告_20260821.md`
- 事实截止时间：2026-08-21（本次新鲜 start-check、清单/参数哈希、accepted 预测分布核验）
- CA2 前置：`CA2代码审计报告_20260821_R2.md`，总体 `CONDITIONAL GO`，硬 Bug 全过；用户接受日期 2026-08-21
- 未绑定 `W###`；只读

## 可检验命题与判定

- 命题：现行 MPP2 下 internal-val≈0.80 与 external XZY≈0.65 的差距，有多少可归因于「患者内空间 10% 验证、同一患者身份进 train 与 val」以及「train-only z-score 的目标/尺度」；是否支持总管去写划分/z-score 部署讨论稿。
- 总体判定：`CONDITIONAL GO`
- 判定覆盖范围（一句话，写明没有覆盖什么）：覆盖「现行协议让内部指标偏乐观，且 train-only z 尺度足以解释 PCC 尚可而 raw R² 为负；值得总管去 `01_指南与解读/部署方案/` 写讨论稿」；**没有**覆盖新实验训练 GO、没有绑定 W008、没有批准某一种新划分/新 z-score、没有用 XZY 选出更好方案，也**没有**把差距量化成可比较百分比。

## 分项结果

| 项 | 硬 Bug | 协议/设计 | 证据类 | 证据位置 |
|---|---|---|---|---|
| 划分单位=空间 block；每患者 1 张切片 | 不适用（与当时协议一致） | WARN：内部 val 是同切片邻域 10% | current | `split_info.json` `block_size=672`；6 人各 1 个 (x,y) 格；230/230 val block 与 train 四邻接 |
| 六患者同时在 train 与 val | 不适用（当时验收就是如此） | WARN：选模信号共享患者身份 | current + accepted | manifest 六人皆有 `train`/`internal_val`；早停=`val_loss`；XZY 不在清单、冻结后评一次 |
| 同一 patch 不跨 split；XZY 不在划分内 | PASS | WARN：跨患者同名 stem 1030 条是坐标碰撞 | current | `patient+patch_stem` 交集 0；`leakage_pairs=0` |
| z-score 只在 train 拟合；val/XZY 只 transform | PASS | WARN：目标/尺度是六人 train raw；clip±100 未触发 | current | `zscore_manifest.json` `fit_split=train`；params sha `fd65f6ab…`；max \|z\|=6.58 |
| 内部 vs XZY 的 z 分布与预测振幅 | 不适用 | WARN：内部≈(0,1)，XZY 均值偏移约 0.9 train-σ、预测更压缩 | accepted | 内部 mean\|μ\|=0.037、σ≈1.01、σ̂/σ=0.82；XZY mean\|μ\|=0.893、σ≈0.76、σ̂/σ=0.59 |
| PCC≈0.65 而 raw R²≈−0.088 | 不适用 | PASS：仿射不变 vs 对均值/尺度敏感，无需硬 Bug | accepted | 内部 PCC 0.797 / R² 0.635；外部 0.655 / −0.088；R²_z=R²_raw |
| 用 val 或 XZY 拟合 z-score | PASS（未发生；若发生则本对话应 NO-GO） | 不适用 | current | rebuild 脚本与 preflight |
| 超参 δ=1 / λ=10 / W007 解耦 | 不适用 | 对照：同一协议下未抬外部 PCC；不得反推新值 | accepted / rejected / pending_review | 总管汇总 B；Ridge 仍 rejected |

## 已核验事实 / 假设 / 不得执行（必须分开）

- **已核验事实：** 现行是六患者内空间 10% block val；checkpoint 来自该 val；z-score 只在 train 拟合；内部标签 z 接近 (0,1)，XZY 不是；Huber/W007/Ridge 未改变「卡住约 0.65」这一形态。
- **假设：** 改患者级 val 可能让内部数字变老实，不保证 XZY PCC 上升；oracle 把预测均值对齐到 XZY 后 R² 可从 −0.088 升到约 +0.27（**用了 XZY 标签，禁止当方法**）；V3bis 为 historical，不得锁定协议。
- **不得执行：** 新 split；重拟合/覆盖 z-score；绑定 W008；写讨论稿正文（交总管）；commit；用 6+XZY 拟合；按 XZY 挑划分；从 XZY 反推超参。

## 给总管的下一允许动作（只选一个）

- [ ] 等待用户审核本回传
- [ ] 授权另开修复代码对话（仅当硬 Bug FAIL）
- [ ] 停止怀疑一般性硬 Bug，允许开启划分与 z-score 诊断对话
- [ ] 超参数审查完成，不授权训练；交总管决定是否纳入后续合同
- [x] 划分/z-score 诊断支持总管去 `01_指南与解读/部署方案/` 写讨论稿（仍不绑定工作树）
- [ ] 证据不足，需要用户补充材料后再审

CONDITIONAL GO 的点名条件：当前 `split_manifest.csv` sha `1f7d1345…` ≠ 训练绑定 `babfa2b1…`；勿把 in-repo 旧冲突 train CSV 当训练输入；讨论稿不得用 XZY 选方案，不得覆盖本套生产划分/z-score；不能把 0.80−0.65 写成已定量的百分比。这些**不是**硬 Bug FAIL。

讨论稿若写，只列选项不锁定：患者留出 val；训练患者 LOPO/nested CV 且 XZY 仍一次性；维持空间划分只改报告口径；新划分下按折重拟合 train-only z-score；患者内相对标准化/排名（改任务定义）。明确不推荐：6+XZY 拟合；按 XZY 挑划分；覆盖旧 manifest。

## 请用户当场批准

- [x] 接受本回传的总体判定（`CONDITIONAL GO`：支持总管写划分/z-score 讨论稿，不是训练 GO）
- [x] 接受“下一允许动作”（总管去 `01_指南与解读/部署方案/` 写讨论稿，仍不绑定工作树）
- [ ] 不接受；需要返工的点：

用户于 2026-08-21 在划分与 z-score 诊断对话中声明「接收判定」，并要求回传总管提示词。

## 明确未做

未修代码、未训练、未 import、未重生成 z-score / 标准划分 / ssGSEA / manifest、未写部署方案、未创建实施方案、未绑定 W008、未 commit、未 push、未 `git add 03审计报告/`、未用 XZY 选择划分或标准化、未从 XZY 反推 Huber δ / Ridge λ / W007 权重。
