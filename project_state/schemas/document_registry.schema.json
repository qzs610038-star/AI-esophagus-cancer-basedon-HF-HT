{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://pfmval.local/schemas/document_registry.schema.json",
  "title": "PFMval document registry",
  "type": "object",
  "required": ["schema_version", "updated_at", "state_revision", "documents"],
  "properties": {
    "schema_version": {"const": "1.0"},
    "state_revision": {"type": "integer", "minimum": 1},
    "documents": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["doc_id", "path", "category", "scope", "authority", "lifecycle", "availability", "verified_at", "state_revision", "supersedes", "superseded_by", "truth_sources"],
        "properties": {
          "authority": {"enum": ["normative", "derived", "reference"]},
          "lifecycle": {"enum": ["draft", "pending_review", "approved_design", "active", "superseded", "historical", "missing"]},
          "lifecycle_override": {
            "description": "显式人工生命周期覆盖；扫描刷新保留已登记的 lifecycle 与 authority，不自动改写人工治理结论",
            "type": ["string", "null"],
            "enum": [null, "draft", "pending_review", "approved_design", "active", "superseded", "historical"]
          },
          "availability": {"enum": ["tracked", "local_only"]},
          "doc_role": {"type": "string", "minLength": 1},
          "related_docs": {
            "type": "array",
            "items": {"type": "string", "minLength": 1},
            "uniqueItems": true
          },
          "covered_documents": {
            "description": "Explicit repository-relative member paths covered for navigation by this existing parent document; does not promote member evidence or delete existing independent entries.",
            "type": "array",
            "items": {"type": "string", "minLength": 1},
            "uniqueItems": true
          },
          "source_refs": {
            "type": "array",
            "items": {"type": "string", "minLength": 1},
            "uniqueItems": true
          },
          "dependency_fingerprints": {
            "type": "object",
            "additionalProperties": {"type": "string"}
          },
          "freshness": {"enum": ["fresh", "review_due"]},
          "freshness_reason": {"type": "string"},
          "changed_dependencies": {
            "type": "array",
            "items": {"type": "string", "minLength": 1},
            "uniqueItems": true
          }
        }
      }
    }
  }
}
