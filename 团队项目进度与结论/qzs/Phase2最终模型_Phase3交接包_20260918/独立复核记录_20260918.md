# PFMval independent audit

更新至 2026-09-19、交接适配版本 1.1.0。本记录已纳入用户授权修复后的证据；以本记录和 `audit_summary.md` 的当前版本为准。

## Claim

- Testable claim: 本包交付的是 Phase2 全视野实验已接纳批次中、预先指定导出的 UNI2-h 空间残差正式权重（种子 45、第 28 轮），其输入、图参数、模型结构和 README 中的单种子指标与登记结果一致；可作为 Phase 3 冻结特征生成器，但不能单独证明跨患者泛化，也不能替代 Phase 3 下游重训。
- Requested decision: 该交接包能否按当前合同发给队友用于 Phase 3。
- Scope and identifiers:
  - experiment_id: `phase2_fullfov_hpo_v1`
  - result_id: `phase2-fullfov-hpo-v1-20260916-231813-result`
  - batch_id: `20260916_231813_101_7b1b9a79`
  - task_id: `final-ablation__uni2h__full_fov_224_bicubic_v1__spatial__45__frozen_spatial`
  - package: `团队项目进度与结论/qzs/Phase2最终模型_Phase3交接包_20260918`
  - 原始结果与权重仅只读核验；用户随后授权修复交接代码与说明，并核对部署方案、实现和产物。未访问服务器原件，未重训，未计算哈希。

## Sources inspected

| Source | Lifecycle | What it proves |
|---|---|---|
| `experiments/experiment_registry.json` 中本条结果 | `accepted` | 本批 111 次训练与 51 份 XZY 正式预测已接纳；接纳范围不含跨患者泛化、物理支持域证明和 Phase 3 合同完成 |
| `analysis/local_metrics.json`、`analysis/external_metrics.json` | `accepted` 的计算来源 | 种子 45 内部 / XZY 两种 PCC 与 pooled z-MSE 的原始复算值 |
| 回传 `train_result.json`、`optimizer_effective.json` | `accepted` 的运行记录 | 训练 60 轮后早停、正式点第 28 轮、Adam 1e-4 / 批 32、17760 次更新 |
| 包内 `weights/formal_best.pt` | `current` 本地载荷 | 检查点元数据、冻结配置与 `model_state_dict` 形状 |
| 包内 `training_reference/effective_config.json`、`frozen_selection.json` | `current` | 冻结空间配方 spatial-11 与生效配置 |
| 包内 `src/phase3_package.py` 与训练 `src/model.py`、`src/graph.py` | `current` | 推理合同与训练前向/边权公式 |
| 包内测试及项目外副本启动 | `diagnostic_only` | 16 passed、无跳过；真实权重＋合成 H5 / 逐 patch 路径通过，不证明真实队列 |
| 原部署方案、实施细则与 111 个任务记录 | `accepted` 产物的复核 | 任务覆盖、配置、实际优化器记录、更新次数、配对初值、逐轮选模与早停一致 |
| 162 份原始预测与补充指标 | `derived` | 独立重算四个已报告指标及种子统计；补充原尺度误差，不新增结果接纳 |

## Checks

| Result | Check | Fresh evidence | Boundary |
|---|---|---|---|
| PASS | 权重身份 | 包内 `formal_best.pt` 存在（4,920,186 字节）。载荷为 `arm=spatial`、`kind=formal`、`seed=45`、`epoch=28`；`selection.score=0.6771945872912521` 与内部验证患者—通路等权 PCC 一致；`spatial_head.weight` 非零且无 bias | 未与服务器原件做哈希比对；身份依据载荷元数据 |
| PASS | 模型设置 | 检查点冻结配置的 data/model/graph/training/selection/preprocessing 与包内 `effective_config.json` 逐段相同。H:1536→256+GELU，C:256→30，B:256→30 无偏置。图：半径 2 步、最多 12 邻、σ=0.5、温度 0.7716396069760084、自身原始权重 0.5 | 常数学习率下 `warmup_epochs=5` 字段存在但不生效（`build_lr_scheduler` 对 constant 直接返回） |
| PASS | 选模未用外部 | `frozen_selection.json` 写明 `external_test_used_for_selection=false`；正式点选自内部验证 | 内部验证参与了搜索，XZY 不是新盲测 |
| PASS | 指标真实性 | README 六位小数与 Registry / 原始复算一致：内部 0.677195 / 0.810774 / 0.352305；XZY 0.568833 / 0.685507 / 0.638409。划分清单训练 9472、内部验证 1078，患者+patch 无重复，train/val 无交集 | README 的 z-MSE 是整体展平 `pooled_z_mse`，不是选模破同分用的患者等权 z-MSE（0.355310） |
| PASS | 推理与训练前向 | 本次 `pytest`：真实权重 + 合成逐 patch 输入下，交接模型与训练 `SoftlinkModel` 逐值相同；输出 `[N,30]` 与融合 `[N,1566]` | 填充邻居靠零权重而非训练时的 mask；孤立点因 B 无偏置，数学上等价于仅用 C |
| PASS | 标准化与通路顺序 | `assets` 与 `training_reference/inputs/mpp2` 的 z-score 文件相同；`fit_split=train`，n=9472；30 通路名称与评分文件一致 | 禁止用 Phase 3 队列重估 mean/std |
| WARN | 输入协议不可从数组反推 | `feature_protocol` 是操作者声明。旧开箱包（2026-09-12）使用半径 1.5 / 8 邻及历史中心裁剪特征。新包必须用 `full_fov_224_bicubic_v1` 的 UNI2-h CLS | 用旧裁剪缓存会静默得到错误邻域与错误特征 |
| PASS / WARN | 切片步长 | 按用户明确要求：缺失、无效或冲突时警告并继续，包括严格模式；稳定推断也会警告。逐切片记录实际值与来源，运行清单保留待核实列表 | 未知步长允许继续，但不宣称几何已核实；后续有可靠值时另建运行重生成 |
| WARN | 本包不能直接复算 Phase 2 验证指标 | 训练时验证图只在 `internal_val` 点之间连边；Phase 3 推理对输入切片上的全部点构图 | 若把训练+验证点一齐送入本包，邻居会与原评估不同 |
| PASS | H5 路径 | 临时依赖环境下已通过真实权重的合成 H5 输入、坐标保留、融合顺序和独立副本启动检查 | 目标环境仍应对真实输入做 preflight |
| PASS | 输入身份与完整性 | 跨患者切片冲突被拒绝，坏行保持其余行对齐，重复切片不覆盖；不完整运行标为 partial 并返回退出码 1 | partial 产物可保留，但不能登记为完整队列 |
| PASS | 缓存路径迁移 | 显式旧路径—新路径映射可以读取搬迁缓存；错误模型、协议和身份顺序仍被拒绝；源缓存未改 | 仅为搬迁适配，未启动单种子训练 |
| PASS | 方案与结果一致性 | 111 次训练记录与方案、记录源码一致；162 份预测独立复算，最大差异 1.11×10⁻¹⁵。详见 audit/experiment_consistency.json | 原图和真实特征缓存不在本机，不能据此声称完整重新运行复现 |
| UNVERIFIED | 物理支持域 | `slide_geometry.csv` 的 `patch_coverage_size` 为空 | 已纳入本批接纳范围的已知限制，不构成实现错误 |
| NOT APPLICABLE | 服务器原件现场存在 | 本次未登录训练服务器 | 路径已写在 `server_locations.json` |

## Verdict

- Overall: `GO`，限当前合同下的代码包分享。
- Reason: 包内权重、冻结配置、图参数、通路顺序与已接纳登记一致；输入错配、部分完成误报及缓存搬迁问题已修复并复验。使用匹配协议的全视野 UNI2-h 特征与新图参数生成特征后，重训 Phase 3 下游。步长未知只警告，不构成停止或再次审批条件。
- Evidence scope: 本地检查点载荷 + 已接纳 Registry/原始预测复算 + 包内测试。不覆盖服务器文件仍存在、真实临床 H5、或 Phase 3 疗效结果。

## Remaining

- Blocking: 本次范围内已证实的交接错误均已修复；没有尚未解决的交付阻断项。
- Non-blocking: 物理 patch 覆盖未核实；输入协议靠声明；未知步长可能改变邻域但会明确留痕；内部验证为同患者不同点位，XZY 为单患者历史外部测试。真实队列需先预检，结果登记以 Registry 为准。
- Delivery: 按当前目录整体复制；无需重新训练 Base，不覆盖旧缓存或旧权重。完整三种子来源见 `assets/seed_sources.json`。
