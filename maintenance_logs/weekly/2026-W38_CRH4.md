# 每周维护 2026-W38｜CRH4（只读代码与仓库卫生检查）

- 结论：WARN（无 P0；1 项 P1；2 项 P2）
- 本周变化：未清理、未覆盖、未提交。用户已有改动保持原样（M 10 个跟踪文件：AGENTS、CURRENT_STATE、Registry/Dashboard/Progress、`current_state.json`、两个 Skill、模板 README；未跟踪 21 条目共 130 文件 2.3MB，最大单文件 0.59MB：新独立包 `experiments/phase2_backbone_point_ablation_20260914`、`experiments/phase2_fullfov_hpo_v1`，Phase2 指南/方案/审计 md，`work/` 3 个评审目录）。相对 W36，pytest 仍为 18/1，P1 未消化。
- 主要风险：
  1. P1｜`tests/test_pfmval_governance_skill.py::test_g08_canonical_skill_and_thin_adapter_expose_seven_safe_routes` 持续失败（W36 起）：HEAD 与工作区版 `.agents/skills/pfmval-governance/SKILL.md` 均不含七路线反引号名，非本周 1 行改动所致 → 整理候选（人工决定测试退役或改写），禁止自动改 Skill 或测试
  2. P2｜共享依赖候选：`phase2_fullfov_hpo_v1/src/run_io.py` 与 `phase2_softlink_local_v2/src/run_io.py` 逐字节相同（293 行）；`load_config` 现有 5 份实现（`config_utils.py` + 4 个实验包）；`compute_metrics` 3 份（`pfmval_core/metrics.py`、mpp2 baseline、backbone `analyze_local.py`）；符合“代码包相对封闭”规则 → 整理候选，禁止自动抽取或改包
  3. P2｜本地体量约 102GB（329 个忽略项）：`uni2h_cache_tokens_aug` 26.9GB、`checkpoints/` 16.2GB（`online_tokens` 13.1GB）、`omiclip_cache` 13.2GB、`pretrained_omiclip` 8.7GB、`histogene/checkpoints` 8.6GB、`experiments/results` 1.9GB；根目录跟踪的 `1.1.0`（0B）、`tmp_article.xml`、`tmp_bioc.json` 疑为误跟踪临时文件 → 仅统计，禁止自动清理
- 证据：
  - `git status --short --branch`：`main...origin/main` 无 ahead/behind；10 M / 21 ??（不含本日志）
  - 活跃 `*.py` 289 个（排除 histogene/egnv1/egnv2/reference_implementations/historical_*/uni2h_new 等）：TODO/FIXME/XXX/HACK 0 命中；breakpoint/pdb 0 命中
  - 无 `import histogene|egnv1|egnv2`；引用仅为路径常量、保护清单与测试断言；`deploy/cmd_server.py:35-48` 仍列 4 个已不存在的 histogene/egnv2 脚本名（沿 W36）
  - 训练入口：根 `train_mpp_uni2h_{mlp,lora}.py` + 8 个 `experiments/*/runner.py`（含模板）；新包 `runner.py` 为模板 +4 行，`dispatch.py`/`config.py` 在两新包间已分化，非重复
  - pytest（`PYTHONDONTWRITEBYTECODE=1`，`-p no:cacheprovider`）：通过 18 / 失败 1；`.pytest_cache`/`__pycache__` 时间戳未变
- 建议：
  1. 人工裁决 G08 测试去留（已跨 2 周）
  2. 若后续再复制 `run_io.py`，由用户决定是否上收至 `pfmval_core`
  3. 缓存/checkpoint 释放与根目录误跟踪文件清理须用户单开任务；histogene/egnv1/egnv2 不得修改
