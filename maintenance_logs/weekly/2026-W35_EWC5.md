# 每周维护 2026-W35｜EWC5

- 结论：WARN
- 本周变化：无上周日志，记为首周基线
- 本周期无结果闭环任务
- 工作树异常：W007 登记 HEAD `b9965464` 与现场 `1d19bc21` 漂移且 `last_verified_at` 空；未注册回传 `D:/AIPatho/qzs/pfmval_result_returns/W007/W007-FBR-R001/git-return`（`configs/server_paths.yaml`→`server_result_returns`）。W004 dirty（未跟踪 `1.1.0`），`close_eligibility=blocked_pending_worktree_review`。另有约 16 个历史未注册工作树；无重复绑定。W002/W003 tombstoned 且现场缺失，符合已撤树。
- job/result 缺口：无。`active_training_jobs=[]`，`pending_result_ids=[]`。W005 `abandoned_reserved` 无 lease/job。`project_state/inbox/W007/quarantine` 仅为已接纳回传残留。
- 资产候选：`pretrained_omiclip/checkpoint.pt`（约 7.6GB，unknown）；`openmidnight/best_mlp_regressor.pth`；`openmidnight/best_mlp_regressor_with_scheduler.pth`
- 主要风险：
  1. W007 身份漂移与未注册回传工作树
  2. W004 dirty 阻断关闭复核
  3. `pretrained_omiclip/checkpoint.pt` 未知大型模型占盘
- 证据：`agent start-check --strict` PASS=13 WARN=2 FAIL=0；`workspace scan --host local` WARN；`asset validate` PASS（shadow）；`asset lifecycle-preview` 6 candidates。Gitee-only：`current_state.server_transport.mode=gitee_only`，远程 `gitee` 在册，未 push。
- 建议：人工核对 W007 登记 HEAD；决定 W004 未跟踪 `1.1.0` 去留；确认 `pretrained_omiclip/checkpoint.pt` 是否保留。只报告，不整改。
