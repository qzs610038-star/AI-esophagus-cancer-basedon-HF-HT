# 每周维护 2026-W37｜EWC5

- 结论：FAIL（`start-check --strict` 结构错误；工作树/资产为 WARN；只报告，不整改）
- 本周变化：相对 `maintenance_logs/weekly/2026-W35_EWC5.md`：start-check 由 PASS=13/WARN=2 变为 FAIL=1；新增未注册 `.claude/worktrees/agent-af83d77f0b14b0e8d`；现行通道已非 Gitee-only。
- 本周期无结果闭环任务
- 工作树异常：W007 登记 HEAD `b9965464` 与现场 `1d19bc21` 漂移且 `last_verified_at` 空；未注册回传 `D:/AIPatho/qzs/pfmval_result_returns/W007/W007-FBR-R001/git-return`（`configs/server_paths.yaml`→`server_result_returns`）。W004 dirty（未跟踪 `1.1.0`），`close_eligibility=blocked_pending_worktree_review`。W001 `blocked_nonterminal_attempt_events`。另约 16 个历史未注册树 + 上述 Claude 工作树；无重复绑定。W002/W003 tombstoned 且现场缺失，符合已撤树。
- job/result 缺口：无 active job，无需批准/`source_commit`。`pending_result_ids=[]`，inbox 无待导入信封。`phase2_softlink_local_v2` 的 `evidence_status=pending` 但原始结果在 `experiments/results/phase2_softlink_local_v2/20260908_005325_810_8d306c10/`，不在 inbox。`project_state/inbox/W007/quarantine` 仅为已接纳残留。W005 `abandoned_reserved` 无 lease/job。
- 资产候选：`pretrained_omiclip/checkpoint.pt`（约 7.6GB，unknown）；`openmidnight/best_mlp_regressor.pth`；`openmidnight/best_mlp_regressor_with_scheduler.pth`
- 主要风险：
  1. `project_state/directives.jsonl` 中 DIR-20260908-003（及 DIR-20260908-004）缺 `effective_from_revision`，start-check 结构 FAIL
  2. W007 身份漂移与未注册回传工作树
  3. `pretrained_omiclip/checkpoint.pt` 未知大型模型占盘
- 证据：`agent start-check --strict` FAIL=1；`workspace scan --host local` WARN；`asset validate` PASS（shadow，0 assets）；`asset lifecycle-preview` 6 candidates、0 writes。Gitee：`current_state.server_transport.current_channel=manual_directory`，`paused_channels=[gitee]`，`configs/server_paths.yaml` `transport.remote=null`；远程名 `gitee`/`origin` 仍在册，本检查未 push。
- 建议：人工补 DIR-20260908-003/004 的 `effective_from_revision`；核对 W007 登记 HEAD；确认 omiclip checkpoint 是否保留。
