# 每周维护 2026-W38｜EWC5

- 结论：FAIL（`start-check --strict` 结构错误持续；工作树/资产为 WARN；只报告，不整改）
- 本周变化：相对 `maintenance_logs/weekly/2026-W37_EWC5.md`：start-check 仍 FAIL=1（同一根因）；`phase2_softlink_local_v2` 的 `evidence_status` 由 pending 变为 accepted，不再有原始结果游离于登记外；新增未登记实验包 `experiments/phase2_backbone_point_ablation_20260914/`（Registry 0 命中，无运行/结果，属准备中；包内含 `.pytest_cache/`、`__pycache__/` 本地产物）；工作树扫描与资产候选清单无变化。
- 本周期无结果闭环任务
- 工作树异常：W007 登记 HEAD `b9965464` 与现场 `1d19bc21` 漂移（现场为线性后代、领先 53 提交，非分叉），`last_verified_at` 空，`close_eligibility=not_evaluated`。W004 dirty（未跟踪 `1.1.0`），`blocked_pending_worktree_review`。W001 clean 但 `blocked_nonterminal_attempt_events`。W002/W003 tombstoned 且现场缺失，符合已撤树。未注册树 18 条：主仓库自身、`D:/AIPatho/qzs/pfmval_result_returns/W007/W007-FBR-R001/git-return`（对应 `configs/server_paths.yaml`→`server_result_returns`，legacy）、`.claude/worktrees/agent-af83d77f0b14b0e8d`、15 个历史 `PFMval_new_*` 树；无重复绑定。W005 `abandoned_reserved` 无 lease/attempt/job。
- job/result 缺口：`active_training_jobs=[]`，无需核对批准与 `source_commit`。`pending_result_ids=[]`；`project_state/inbox/` 仅 `W007/quarantine/{CPGCR,FBR,HCR,RCC}` 已接纳残留（约 73 MB，含 4 个 `best_checkpoint.pth`），无待导入信封。Registry 标签滞留：W006 `gitee_roundtrip_pilot_w006_v001_20260810` status=closed/evidence_status=pending（零训练试点）、`S1b_gfnet_lora_r8_cls_pool8x8_fold1` paused/pending（历史）；均不在 `pending_result_ids`，无缺失原始结果文件问题。
- 资产候选：`pretrained_omiclip/checkpoint.pt`（约 7.66 GB，era uncertain，evidence_role unknown）；`openmidnight/best_mlp_regressor.pth`；`openmidnight/best_mlp_regressor_with_scheduler.pth`。`project_state/asset_registry.json` 登记 0 条（shadow），受保护资产仅由 2 条编译策略规则覆盖。
- 主要风险：
  1. `project_state/directives.jsonl` 中 DIR-20260908-003 与 DIR-20260908-004 仍缺 `effective_from_revision`，start-check 结构 FAIL 连续两周
  2. W007 登记 HEAD 陈旧（落后现场 53 提交）且未注册回传树仍在，工作树身份未闭合
  3. `pretrained_omiclip/checkpoint.pt` 未知大型模型占盘约 7.66 GB
- 证据：`agent start-check --strict` FAIL=1；`workspace scan --host local` WARN（18 unregistered，0 failures）；`asset validate` PASS（shadow，0 assets）；`asset lifecycle-preview` 6 candidates、0 writes。Gitee：Gitee-only 往返规则已被 DIR-20260905-002 取代（`current_state.superseded_conclusions`）；`current_state.server_transport.current_channel=manual_directory`，`paused_channels=[gitee]`；`configs/server_paths.yaml` `transport.current_channel=manual_directory`、`remote=null`；远程 `gitee`/`origin` 仍在册，`main` 与 `origin/main` 0/0，本检查未 fetch/push。检查前后 `git status` 无差异，除本日志外无文件修改。
- 建议：人工补 DIR-20260908-003/004 的 `effective_from_revision`；核对并更新 W007 登记 HEAD 与 `last_verified_at`；决定 `pretrained_omiclip/checkpoint.pt` 保留或归档。
