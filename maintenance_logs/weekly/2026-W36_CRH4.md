# 每周维护 2026-W36｜CRH4（只读代码与仓库卫生检查）

- 结论：WARN（无 P0；1 项 P1；2 项 P2）
- 本周变化：未清理、未覆盖。用户已有改动保持原样（M：`.gitignore` +8 行，独立实验包跟踪例外；未跟踪：Phase2 指南/方案 md 约 0.22MB、`experiments/mpp2_uni2h_mlp_baseline_20260906` 2.23MB 含 `tests/work` 1.53MB、maintenance_logs 2 个小文件）。相对 W35，pytest 由 19/0 变为 18/1。
- 主要风险：
  1. P1｜`tests/test_pfmval_governance_skill.py::test_g08_canonical_skill_and_thin_adapter_expose_seven_safe_routes` 失败：现行 `.agents/skills/pfmval-governance/SKILL.md` 已精简，不含七路线与 `--task knowledge` 正文 → 整理候选，禁止自动改 Skill 或测试
  2. P2｜训练入口并存：根目录 `train_mpp_uni2h_mlp.py`（argparse）与独立包 `experiments/mpp2_uni2h_mlp_baseline_20260906`（config.json）；`train_mpp_uni2h_lora.py` 目标不同、非重复 → 整理候选，禁止自动删除
  3. P2｜缓存/检查点体量：`uni2h_cache_tokens_aug` 26.87GB、`checkpoints` 16.2GB（`online_tokens` 13.1GB）、`omiclip_cache` 13.15GB（含 `JFX0729_old_20260616` 5.72GB）、`histogene/checkpoints` 8.64GB → 仅统计，禁止自动清理
- 证据：
  - `git status --short --branch`：`main...origin/main`，无 ahead/behind；pytest 后无新增缓存/pyc
  - 活跃代码 TODO/FIXME/HACK/breakpoint/pdb：0 命中（`import pdb` 仅在 `reference_implementations/` 与团队 CLAM 副本）
  - 活跃 `*.py` 无 `import histogene|egnv1|egnv2`；`get_histogene_dir`/`get_egnv2_dir` 无调用方；`deploy/cmd_server.py:33-48` 仍列已缺失的 histogene/egnv2 脚本名（该文件标明已禁用）
  - 配置：`config_utils.py` 几乎不被训练入口使用；多数脚本自带 argparse 或包内 json
  - pytest（`PYTHONDONTWRITEBYTECODE=1`，`-p no:cacheprovider`）：通过 18 / 失败 1
- 建议：
  1. 人工决定 G08 测试退役或改写，勿自动改 Skill
  2. 明确根目录 MLP 脚本与独立实验包的并存策略
  3. 旧缓存释放须用户单开任务；histogene/egnv1/egnv2 不得修改
