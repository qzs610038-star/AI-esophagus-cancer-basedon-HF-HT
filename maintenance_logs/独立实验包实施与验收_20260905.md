# 独立实验包实施与验收记录

2026-09-05；决定 DIR-20260905-002；当前状态版本 257。

## 已实施

- 更新核心规则、项目治理/审查技能、服务器配置、当前状态与文档导航；退役工作树默认规则，Gitee 暂停，GitHub 仅用于用户触发的备份与网页查阅。
- 新建 `experiments/_template/` 自包含模板：包元数据、配置、依赖说明、PowerShell 启动器、Python 日志入口、零训练演示及最简启动验收。
- 服务器新增路径配置 `D:\AIPatho\qzs\code` 与 `D:\AIPatho\qzs\runs`，保留旧路径值和传输配置为历史。未连接服务器，未实际创建服务器目录。
- 放行指定项目目录 Markdown 正文，更新四个目录索引；未改写团队成员材料正文。原始运行数据、权重、缓存与归档继续忽略。
- 原工作树模板、自动化/同步说明、Gitee 试点与旧治理方案保留正文并标记历史；旧登记器保持不变，新手动回传不伪造工作树/提交/哈希字段。

## 验收证据

本机使用 Python 3.13 和 Windows PowerShell 5.1 运行零训练测试，复制后的代码位于项目 `work/manual_package_validation/` 内的中文、空格路径，不导入仓库公共代码。

| 检查 | 结果 |
|---|---|
| 正常结束、中文标准输出、原始演示产物、空指标与成功退出码 | PASS |
| 主动 RuntimeError，错误日志/合并日志与失败退出码 | PASS |
| 解释器不存在，启动错误日志和 failed 状态 | PASS |
| 每次独立运行目录；替换代码版本后旧运行文件逐字节保持不变（未计算哈希） | PASS |
| current_state 与 document_registry 的现有 JSON Schema | PASS |
| YAML 路径、包解释器与运行根目录、手动传输状态一致 | PASS |
| 指令 JSONL 可解析且新决定仅出现一次 | PASS |
| 12 项代码/Markdown 路径允许跟踪，14 项运行产物/附件/秘密路径仍被忽略 | PASS |

测试入口为 `experiments/_template/tests/test_launcher.py`；配置与忽略规则检查记录在 `work/manual_package_validation/config_checks.json`。临时运行产物保留供复核，不纳入 Git；未运行全项目测试。

状态检查曾发现“待审方案”栏不接受 historical 状态，已将退役方案移到历史栏，保留原记录；复验通过，未放宽现有 Schema。

## 使用与限制

下一轮复制 [模板](../experiments/_template/README.md)，填入实际训练入口、依赖和必要数据合同，再由用户上传。模板演示不代表训练已实现或科研结论成立。

解释器配置来自本项目 Windows 服务器记录，尚未在服务器验收。配置解析失败、输出磁盘不可写、断电或强杀等情况仍可能需要终端信息；不将陈旧 running 状态当作存活或成功证据。

本次无真实训练、无旧实验搬迁、无原始结果删除、无服务器操作、无哈希计算、无 Git 提交推送、无定时智能体创建。Markdown 现已允许跟踪，但尚未提交或上传到 GitHub。
