# 规则与技能自检修复维护记录

日期：2026-09-12

## 范围与结论

按用户要求对项目约束、本机全局规则、项目 Skill、全局 Skill 与 MCP 相关表述做自检，并修正互相矛盾、限制发言/强制门禁、描述不清处。本记录只证明规则与副本对齐修复，不构成科研或训练结论。

总体判定：`CONDITIONAL GO`（已修当前会话可改文件；插件缓存中的 Superpowers 硬门禁未改写，改为由全局/项目优先级覆盖说明消化）。

## 发现（按严重度）

### FAIL → 已修

1. **Cursor 全局规则过期且误标“完全一致”**  
   `C:\Users\33696\.cursor\rules\global.mdc` 仍停留在 2026-07-09 口径，却声称与 Codex `AGENTS.md` / Claude `CLAUDE.md` 完全一致。实际强制“每次会话开始/结束必须调用 agentmemory”、旧步骤命名规则、过简的“杜绝防御性编程”等，与 2026-09-11 后的 Codex 规则冲突，并限制正常发言与任务推进。

2. **Cursor 技能副本相对 `.agents` 主源漂移（4 项）**  
   - `context-gate`：仍用关键词机械触发，并错误假设 `CLAUDE.md` 已加载。  
   - `qmd-search`：强制先 `qmd_query` 读规则，缺少不可用时 WARN 降级。  
   - `grill-me`：退化为 stub（`disable-model-invocation: true` + “Call grilling”），实质限制澄清访谈能力。  
   - `deep-research`：旧 description 用“研究/research”等词触发，与主源“仅显式深研请求才启用”冲突。

### WARN → 已用优先级说明消化 / 部分修

3. **第三方插件 Superpowers**（`using-superpowers` / `brainstorming`）含 HARD-GATE：设计未批准不得实现、会话开始强制读技能。按既定政策**不修改插件缓存**；已在项目 `AGENTS.md`、全局规则、`pfmval-governance` / `pfmval-audit` 写明：用户指令与项目规则优先，任务已明确时不得被插件硬门禁打断。

4. **codebase-memory MCP** 工具描述写 “Use INSTEAD OF grep/glob”，但本仓库无 `.codebase-memory/artifact.json`。已在全局规则明确：无产物则禁用强制依赖并降级；工具文案不得阻止普通搜索。

5. **Claude `CLAUDE.md`** 与 Codex/Cursor 全局规则职责不同（目录速查 vs 行为规则）。已加页眉说明，避免再被写成“完全一致”。

### PASS（无需改）

6. 项目 `AGENTS.md` 与 `.agents/skills/pfmval-*` 主体已与 2026-09-08/09-11 交付方式一致；`.claude/skills` 适配器仍为薄跳转。  
7. `.agents` ↔ `.codex/skills` 对本次核对的关键技能哈希一致。  
8. `deploy/SYNC_GUIDE.md` 顶部已标明 historical / superseded。

## 已执行修正

| 文件 | 动作 |
|---|---|
| `C:\Users\33696\.cursor\rules\global.mdc` | 重写对齐 Codex 偏好；取消强制每会话记忆；明确插件硬门禁与图谱降级 |
| `C:\Users\33696\.codex\AGENTS.md` | 同步日期、插件硬门禁说明、Cursor 兼容副本、记忆非强制、图谱降级 |
| `C:\Users\33696\.claude\CLAUDE.md` | 增加“非行为规则正文”页眉 |
| `C:\Users\33696\.cursor\skills\{context-gate,qmd-search,grill-me,deep-research}\SKILL.md`（及 grill-me/deep-research 必要附属） | 从 `.agents` 主源覆盖同步 |
| `D:\...\PFMval_new\AGENTS.md` | 明确插件硬门禁与 MCP 文案不得覆盖用户指令 |
| `D:\...\PFMval_new\.agents\skills\pfmval-governance\SKILL.md` | 同上 |
| `D:\...\PFMval_new\.agents\skills\pfmval-audit\SKILL.md` | 同上 |

未修改：插件缓存、实验 Registry、团队正文、原始结果/权重、服务器。

## 验收核对

- 四项 Cursor 技能与 `.agents` `SKILL.md` 哈希一致。  
- 项目内 Claude 适配器仍指向 `.agents` 正文。  
- 本仓库不存在可用 codebase-memory artifact → 按规则应降级文件搜索。

## 保留限制

- Superpowers 插件正文仍含 HARD-GATE；新会话若插件被自动注入，依赖全局/项目优先级条款覆盖，未做插件内改写。  
- 未全量审计全部飞书/ Lark 技能中的“写前确认”（属领域高风险写操作，不视为与科研治理冲突）。  
- 未运行全量 skill-sync 工具；本次为定向同步 + 规则对齐。
