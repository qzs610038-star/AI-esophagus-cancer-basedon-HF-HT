# AGENTS.md

本文件只维护项目最核心的长期边界。平台及工具约束优先；当前用户明确指令优先于历史方案、项目一般规则和技能说明。在各自项目范围内，项目具体规则优先于全局一般规则。技能（含第三方插件技能）不得增加无必要的审批、确认、暂停或强制访谈；已明确授权持续有效。任务已明确时，不得因插件“设计门禁 / 强制先读技能 / 强制记忆召回”等表述打断已授权工作；若仍被触发，应说明具体规则来源及其影响后继续执行用户指令。历史方案、工具惯例和可变项目状态不是强制规则；需要时按任务范围读取相关事实源。旧流程不覆盖下列核心约束及当前实验交付方式；Registry、诊断等按需使用，不得自行阻断用户已明确授权的工作。MCP 工具描述（如“优先于 grep”）不得覆盖“无图谱产物则降级”或用户已明确的检索方式。

## 核心约束

1. **用户授权与作用域**：只执行用户指令范围内的操作。训练、外部系统写入、发布以及明显扩大任务范围的操作需要用户明确指令。`团队项目进度与结论/` 根据用户指令修改，智能体不自主修改。
2. **非破坏性操作**：不得自主删除、覆盖或不可逆迁移原始数据、实验结果、checkpoint、缓存、权重和未授权文件；已明确授权的代码和文档可以修改。确需执行其他破坏性操作时，先核对精确目标并取得用户明确指令。
3. **科研有效性**：避免患者、切片和数据集泄漏；监督学习预处理与模型选择只能使用训练集或内部验证集，不得使用外部测试集拟合参数、选择 checkpoint 或调参。其他性能阈值和实验路线门控按 `WARN` 继续已授权任务；用户明确的停止条件仍须执行。
4. **事实与结论诚实**：不得虚构运行、结果或批准；明确区分计划、探索结果、待审结果和已确认结果。文件存在、代码实现或测试通过不自动等同于实验结论成立。

## 当前实验交付方式（2026-09-08）

- 工作树规则彻底退役，仅作历史追溯；只有用户明确要求才创建工作树，不能自动创建或推荐为默认步骤。旧工作树和结果保留，不自主清理。
- 新实验包直接在当前项目 `experiments/<实验名>/` 内编写，并以 `experiments/_template/` 开始；修复既有实验包时只做授权的定向修改，不重复复制模板。代码包相对封闭，包含必要项目代码、小型配置与划分，外部大数据及权重由配置引用。
- 本地回传结果与代码包分离：新结果统一保存到 `experiments/results/<实验名>/<运行编号>/`，派生的本实验初步结果分析写入对应运行目录的 `analysis/`。复杂分析，如跨实验对比、深度机制探索等则写入项目分析报告文件夹内。既有结果保留原存储地址，不自动迁移；Registry 或其他记录继续指向实际原路径。
- Windows 服务器只使用本项目 `configs/server_paths.yaml`：`D:\AIPatho\qzs\code` 放代码，`D:\AIPatho\qzs\runs` 放分实验、分运行的日志和原始结果。用户手动上传/回传，修复只替换对应代码目录，保留运行目录。
- 自 2026-09-08 下一次运行起，新模型权重单独写入服务器 `D:\AIPatho\qzs\weights\<实验名>\<批次>\<运行>\`，常规回传不复制权重文件。回传结果必须包含 `model_weights.json` 等路径登记，逐运行记录服务器权重目录及 warmup/formal/last checkpoint 绝对路径。既有回传与权重不改动。
- 自 DIR-20260912-001 （2026-09-11）起，对**后续新实验/新运行**生效：新提取的特征缓存写入服务器 `D:\AIPatho\qzs\feature_caches\`（可按实验或来源分子目录），**不得再写入 `runs`**；常规回传**不复制特征缓存文件本体**，但必须在对应运行目录保留并回传路径登记（如 `feature_caches.json`），逐项记录所用/新写特征缓存的服务器绝对路径，便于后续查找。旧特征缓存路径继续保留可用，不退役、不强制迁移；新旧缓存可分目录并存，既有实验与既有回传保持现状不改。
- Gitee 同步暂停；旧通道和脚本仅供追溯。GitHub 仅用于备份和网页端探索，提交与推送由用户触发。实验前不主动检查 Git 干净状态、不运行 start-check；仅在跨模块结构重写、批量迁移替换等改动前，先只读列出改动清单，再由用户触发备份；普通定向规则维护不适用，不自动提交或推送。
- 服务器仅产出原始训练输出及训练、早停、模型选择必需指标；可由原始数据推导的其余指标、图表与登记在本地完成。既有“全部指标保留”继续有效，仅改变计算位置。
- 学习、探索、决策、团队贡献等指定文档目录中的 Markdown 正文允许纳入 Git；跟踪不代表提交已发生，也不代表科研结论获确认。团队正文仍只根据用户指令修改。
- 操作说明见 [独立实验包与手动回传](project_state/governance/独立实验包与手动回传_20260905.md)。

## 任务相关事实源与建议阅读

只读取当前任务涉及的文件和字段，不要求每次会话全量加载。


| 涉及事项               | 建议优先读取                                                                     | 说明                                                   |
| ------------------ | -------------------------------------------------------------------------- | ---------------------------------------------------- |
| 项目当前状态、进行中事项和待处理事项 | `project_state/current_state.json`                                         | 当前状态机器源；`CURRENT_STATE.md`、`PROJECT_GUIDE.md` 仅作人工概览 |
| 已登记实验及正式结果         | `experiments/experiment_registry.json`                                     | 实验与结果主要事实源，只读取相关 experiment/result                   |
| 实验进度和结果汇总          | `experiments/experiment_progress.md`、`experiments/experiment_dashboard.md` | 派生视图，便于浏览，不覆盖 Registry                               |
| 用户历史决定和规则变更        | `project_state/directives.jsonl`                                           | 仅在需要追溯批准、暂缓或取代关系时读取                                  |
| 服务器路径和当前同步方式       | `configs/server_paths.yaml`                                                | 仅在服务器、路径或同步任务中读取                                     |
| 论文、方案和其他项目材料       | `project_state/document_registry.json`                                     | 用于定位与任务相关且当前有效的文档                                    |


## 实验结果登记与阅读

自2026-09-08起，实验注册和分析报告必须同时并列两种PCC，并分别报告内部验证、外部测试及可用的种子均值/标准差：①逐通路平均 PCC（即患者—通路等权平均 PCC）：逐患者逐通路PCC后等权平均；②平均池化 pooled PCC（整体展平 PCC）（项目既有`pooled_pcc`/`flattened_pcc`口径，全部点位×通路的z分数展平后计算一次相关）。明确两者聚合与标度，不能以数值高低互证优越。若另算合并点位后逐通路平均PCC（`pooled_pathway_pcc`），单独标注，不与整体展平口径混用。此展示要求不改变既定模型选择指标。

手动回传时先保存原始输出，在本地补充派生指标并登记；服务器不生成旧登记信封。旧导入器若要求 W###、提交编号或哈希，不伪造字段、不自动计算哈希，可根据用户授权直接维护相关登记，结果未经确认不得标为 accepted。

历史兼容导入流程（已有对应元数据时按需使用）：保留原始运行输出和必要指标；按 `project_state/schemas/result_envelope.schema.json` 或 `result_envelope_v2.schema.json` 形成结果描述；将待登记结果放入 `project_state/inbox/`；使用 `python deploy/pfmval_ops.py result import ...` 导入；导入后以 `experiments/experiment_registry.json` 为准，并按需更新派生视图。

- 未登记结果可以分析，但必须标为探索结果或待登记结果；只有 Registry 中明确标为 `accepted` 的结果才能称为已确认结果。
- 涉及性能、实验状态或下一步决策时，先核对 Registry 中对应记录；Dashboard、报告、聊天摘要和文件名不能单独证明实验已完成或已接受。
- 原始结果与 Registry 不一致时应报告差异，不得静默覆盖。登记流程用于维护事实边界，不把固定格式、哈希、W### 或同步通道重新设为通用硬门禁。

## 当前工具与配置

- 当前状态、实验与文档 Registry 用于按需检索。工作树仅保留历史，创建须用户明确指令；start-check 不作为日常实验前步骤。结构损坏或未完成写事务可阻止继续写入，其余不一致原则上报告为 `WARN`。
- 当前通道是用户直接复制文件夹（manual_directory）；交付时给出需要复制的准确文件夹路径。默认不在本地或服务器生成压缩包，需要时由用户自行压缩，或在用户明确要求后再生成。Gitee 暂停，SSH 暂不使用；不自动进行任何远端同步。普通定向规则维护无需备份提醒。
- 不默认计算哈希；确需启用时先说明原因、必要性及影响，得到用户明确批准后再做。旧格式有哈希字段不构成自动授权。
- 具体服务器路径、实验状态和论文材料仅在任务相关时读取；派生视图、历史记录和旧方案不得覆盖用户当前指令或实际证据。

