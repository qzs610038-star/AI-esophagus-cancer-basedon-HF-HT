嗯，下面我来说明一下我准备对软连接通路的改造一些流程。  第一个是我想参考PEARL这篇论文，我们对于对比学习这一块，我们不再是单独只训练回归，而是会拿对比学习内容去训练第一层MLP网络，就是把1536维映射到1024维再到30维的这个两层MLP网络，让它和通路进行对齐。  还有一些值得修改的点，就是前面提到的我们的隐藏层数量1536~1024再到30。我在想这个是不是可以修改，但是我目前还没有具体的论文查有什么，我还不太清楚隐藏的数量如何配置比较优秀，在我们的这个项目或者在类似计算机视觉项目中，所以请你帮我调研一下隐藏层可以怎么改。然后第二就是软连接对比下面我来说明一下我准备对软连接通路的改造流程：



1. 引入对比学习与网络对齐

我想参考 PEARL 这篇论文，在对比学习这一块，我们不再单独只训练回归，而是会拿对比学习的内容去训练。具体是把MLP 网络（即把 1536 维映射到 1024 维，再到 30 维的两层 MLP 网络）与通路进行对齐。



2. 隐藏层数量配置调研

前面提到的隐藏层数量（1536 → 1024 → 30）是值得修改的点。我在想这个是不是可以修改，但我目前还没有查阅具体的论文，不太清楚在我们的项目或类似计算机视觉项目中，隐藏层数量如何配置比较优秀。所以请你帮我调研一下，隐藏层可以怎么改。



3. 软连接对比权重分配调整

之前好像提到过软连接对比，我们在权重分配上存在一些问题：哪怕某个通路确实与另一个通路的重叠率比较高，也会给其他通路分配一些奇怪的权重。所以我想在这方面也做出一些调整修改。第三步：关于空间信息的使用

我现在考虑到一个问题：如果我们继续采用 PEARL 那种 Transformer，由于我们现在的 phase2 训练集与内部测试集与 PEARL 不太一样（他们是 5 折留出患者集，而我们是把几个患者打散一起输入），所以我们不太好直接像  Transformer 那样完整捕捉训练集。因为这样捕捉的话会出现问题：不同患者之间的空间关系是不同的，空间关系肯定要在同一个患者之内使用才比较靠谱，不应该强加不同患者间的空间关系进去。所以我在想，是不是可以考虑通过其他论文提到的图网络或类似的操作，来利用好我们的空间关系？



第四步：关于模型训练参数量的大小问题

我们之前直接微调模型，只要参数量上去，效果和性能一直不佳。我在想我们这一次要怎么设计：

1. 能不能减少回归头模型参数量？我们可以通过前面的隐藏层参数降低之类的操作，看能不能减少一些，然后再看一下，有哪些地方可以把模型参数量稍微提高一点。
2. 关于残差头对比学习方案：目前的对比方案主要用在残差头上。我现在想把它用到最开始的通路与图像对齐上。关于残差头这部分，我在想：

(a) 还有没有必要加？

(b) 如果加的话，我们用什么信息来训练残差头？是空间信息还是什么样的信息呢？



目前这些我都还不太确定，所以想请你帮我整理一下。



最新阅读收获：  
现在有个比较关键的问题：不管是图网络或超图网络的构建，还是在 Transformer 中引入机制对位置进行编码，亦或是把位置编码输入到我们的对比图像与通路分数的对比学习模块中，都面临同一个问题——九成九的论文都使用的是“留一交叉验证”的对比方法。

放在我们的项目中，就是选五例患者作为训练集，一例患者作为内部验证集，反复进行这样的留一验证操作，从而对患者进行更加深刻、准确的超患者泛化能力验证。

但是，这存在一个问题：我们的 Phase 2 的输出还是要作为 Phase 3 的输入。如果我们采用这种“6折留一”训练方法的话，会面临以下两个问题：

1. 我们不能确定需要把哪一折作为最终指标，提供给 Phase 3 使用。或许可以只用“六折留一”作为一个破坏说明，证明破坏者的破坏能力较强。

对于 Phase 3，可以单独训练一个模型：

1. 使用我们的 6 例作为完整的训练集进行训练；

2. 使用 1 例患者作为验证集进行微调；。

这样数据量大之后，放到 Phase 3 里面效果会不会更好？但这样好像和我们前面“六折留一”的方式又不同了，感觉在科研论证上不太严谨。

2. “6折留一”对训练时间的开销占用比较大。此外，如果不采用“6折留一”，而是对现在的这 6 例患者进行内部随机划分训练集和验证集的话，图网络都不太好引入。因为训练集内部掺杂了这 6 名患者的数据，你都不知道该怎么给你的图网络。不管是图网络的构建还是超图结构的构建，你都要依赖于每一例患者内部构建的空间关系吧？如果患者内部构建的空间关系显得不准确，肯定是不行的。

至于对比学习，你还可以考虑一下跨患者进行对齐，这还有一些泛化作用。但空间关系，我现在想不到比较好的引入方法。

