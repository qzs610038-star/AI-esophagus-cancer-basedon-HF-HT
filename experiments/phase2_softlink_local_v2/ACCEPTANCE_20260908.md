# Phase2 软连接 v2.1 代码验收

验收范围：独立代码包、统一/分实验入口、最小行为测试和科研边界；未启动正式训练，未产生性能结论。

## 结论

- **代码验收：PASS**。服务器输入准备、统一入口、4个实验臂入口、按种子入口、预检、冻结端点外部预测及本地分析均已实现。
- **正式训练就绪：CONDITIONAL GO**。代码可复制到服务器；先运行 `prepare_inputs.ps1` 自动生成 slide 分组、slide 几何与XZY无标签点表，再运行预检。活动 labels 与 UNI2-h 特征缓存仍须以服务器实存为准。
- 当前缺口保持显式失败，不用 `patient_id` 伪造 `slide_id`，不回退旧标签，不用外部标签构图或选点。

## 验收证据

- `python -m pytest -q --tb=short`：43项通过。
- `python -m compileall -q src`：通过。
- 12个 PowerShell 脚本经语法解析：通过。
- 静态核对：训练9472点、内部验证1078点、30通路、正式任务4臂×3种子共12项。
- 调度契约：批次配置与源码快照冻结；任务首次失败后剩余项记为 `not_run`；不自动续训或查找最新检查点。
- 选点契约：第6轮起选正式检查点；外部预测拒绝 warmup/last，只接受明确列出的 formal 检查点。
- 数据契约：身份键为 `(patient_id, slide_id, spot_id)`；图只连同slide同split；外部预测接口不接收标签；标准化只逆变换一次。
- 未计算哈希，未提交或推送 Git，未运行正式训练。

## 启动前警告

1. 本机没有服务器特征缓存，因此交付目录中的映射和XZY点表仍保持未生成状态；复制后运行 `prepare_inputs.ps1`。
2. 若生成入口不能证明每名患者只有一个完整特征来源组，必须停下核实，不能自动合并空间来源。
3. `data.labels_root` 与特征缓存是服务器路径，本机不存在；准备/预检会在服务器现场核实。

代码通过只证明实现合同通过，不代表实验结果已完成、已登记或已接受。
