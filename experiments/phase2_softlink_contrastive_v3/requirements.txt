# Server runtime: Python 3.10, PyTorch 2.6.0 (CUDA build is selected by the
# server image).  Install dependencies before training; this package never
# installs packages or accesses the network from a launcher.
torch==2.6.0
numpy>=1.24,<3
pandas>=2.1,<3
torchvision==0.21.0
timm>=1.0,<2
Pillow>=10,<12
PyYAML>=6,<7

# Development/test dependency (not required by the launcher itself):
pytest>=8,<9
