# Record only. Launchers never install or upgrade packages.
# Server evidence on 2026-09-05: Python 3.13.5, torch 2.6.0+cu124, timm 1.0.27.
numpy>=2.0,<3
pandas>=2.0,<3
Pillow>=11,<13
torch>=2.6,<2.7
torchvision>=0.21,<0.22
timm==1.0.27
safetensors>=0.4,<1
huggingface_hub>=0.30,<2
# Optional: if unavailable, the whole search stage uses deterministic random candidates.
optuna==5.0.0
pytest>=8,<10
