# Phase 2 / MPP2 审计未能正式推进：阻塞汇总

- 日期：2026-08-21
- 总管对话：审查两份查找回传，定位失败原因，并给出治理修复提示词
- 本文件是唯一纳入 Git 的本轮摘要；完整审计工作稿留在本地 `03审计报告/`，不提交

## 1. 发生了什么

查找对话 A（CA2）与对话 B（超参数）都跑完了流程，但**都没有形成可晋级的正式审计结论**。

| 对话 | 正式报告（本地，不提交） | 查找对话自己的判定 | 六项 CA2 / 训练授权 |
|---|---|---|---|
| A CA2 代码审计 | `03审计报告/20260821_Phase2-MPP2-PCC瓶颈/CA2代码审计报告_20260821.md` | `NOT APPLICABLE` | **未出具**。提示词要求 start-check 失败则停止，因此六项硬 Bug 全部 `missing` |
| B 超参数审查 | `03审计报告/20260821_Phase2-MPP2-PCC瓶颈/超参数审查报告_20260821.md` | 审查 `CONDITIONAL GO`；训练 `NOT APPLICABLE` | 超参表已写完，但因同一 start-check 失败，不得当作可执行治理结论去修状态或开训 |

两份回传勾选的下一动作都不是“停止怀疑硬 Bug”，也不是“授权修 MPP2 代码”。CA2 明确要求先处理门禁，或由用户书面授权在 FAIL 仍在时继续核验。

## 2. 失败原因（门禁，不是通路模型硬 Bug）

两次查找都在同一 HEAD 上执行：

- 分支：`main`（相对 `origin/main` ahead 2）
- HEAD：`f02d528c49dad95b3bbc3745e553c26c87723f47`
- `current_state.source_commit`：`dbb196ba205127cf8361d5bd5bf458cfcb5cdf78`（落后 HEAD 2 个治理提交）
- dirty：仅未跟踪 `03审计报告/`
- 命令：`python deploy/pfmval_ops.py agent start-check --strict`
- 结果：`PASS=11 WARN=1 FAIL=2`，`[BLOCKED] Resolve critical state conflicts before continuing.`

HEAD 相对 `source_commit` 的两个提交是治理变更，不是 MPP2 训练代码：

- `05f86ac` 精简项目 Skill 目录（改写了 `document_registry.json`）
- `f02d528` 从 start-check 去掉 skill / AGENTS.md content-hash 门

### FAIL-1：`document_registry_sha256` 未回写

- 检查：`current_state.source_hashes.document_registry_sha256` 必须等于仓库内该文件当前语义哈希
- expected（state 仍记 revision 242 旧值）：`41a4af03480cda644b8c610fd2361f2fdeb66cefc1552bcc10db1cc362638da3`
- actual（当前 `document_registry.json`）：`0571549ad3846de63207eeff428db45f5fed62b90eee495e24240eea1f63137b`
- `experiment_registry_sha256` **一致**，实验登记未漂
- 定性：状态包自洽问题。两个治理提交改了文档登记，没有 `state sync` 把哈希写回 `current_state.json`

### FAIL-2：失效 Temp worktree 使治理校验崩溃

- 检查：`git worktree list` 每条路径会 `Path.resolve(strict=True)`
- 失效登记：`C:\Users\33696\AppData\Local\Temp\pfmval-w004-governance-f8a1c2f`
- 状态：目录不存在，git 标为 `prunable`；校验抛出 `[WinError 2]`
- 持久 W004：`D:\AI空间转录病理研究\PFMval_new_governed_workspaces\W004` 仍在，不是这条 FAIL
- 定性：本地 git worktree 元数据残留，与 MPP2 训练源码无直接关系

### WARN（不阻塞）

`[WARN] MPP standardized labels contain duplicate/conflicting barcodes`  
已知保护资产告警。修复对话**不得**重生成标签。

## 3. 这两份报告已经证明了什么、没证明什么

已经证明：

- 在当前 HEAD 上，**不能**按 AGENTS.md / pfmval-audit 出具项目级 GO/NO-GO
- 阻塞点在状态哈希与 stale worktree，不在 `dataset_mpp.py` / W007 实验代码本身（CA2 尚未打开那些文件做六项判定）

尚未证明（因此不能进入划分诊断，也不能停止怀疑一般性硬 Bug）：

- CA2-1 到 CA2-6 任一项 PASS 或硬 Bug FAIL
- 外部 PCC≈0.65 的科学原因

超参数审查在门禁失败的前提下仍完成了只读表，意见仅供总管汇审，**不授权训练**：

- 冻结基线 lr / epoch / patience / min_delta / val_loss 选模 / batch / Adam：合理
- Huber δ=1、Ridge 选 λ=10（1/30 通路启用）、W007 对比项与回归选模解耦：存疑，未升到“必须改”
- LoRA r=8 smoke：只附表，已是 `no_go_formal`，禁止建议正式重跑

## 4. 正确修复范围

另开**治理维护**对话，只做两件事，使 `start-check --strict` 在允许 barcode WARN 的情况下不再 FAIL：

1. 修剪已不存在的 Temp worktree 登记（保留真实 W004）
2. 用官方 CLI 同步 `current_state` 的 `source_hashes` / `source_commit`（不要手改 JSON）

修复通过后，再另开对话重跑 CA2 六项。不要把治理修复和 CA2 源码审计混在一个对话里。

---

## 5. 修复对话提示词（复制到新对话）

从下面「复制起点」复制到本节结束，作为新对话第一条用户消息。

### 复制起点

你是 PFMval 的**治理门禁修复**对话，不是 CA2 代码审计，不是超参数审查，不是总管。

阅读 `01_指南与解读/分析报告/20260821_Phase2-MPP2审计未能正式推进_阻塞汇总.md` 第 1–4 节后执行。完整审计正文在本地 `03审计报告/`，只作对照，不要提交、不要改写其结论。

#### 1. 目的

两份查找审计因 `python deploy/pfmval_ops.py agent start-check --strict` 出现 FAIL=2 而无法正式推进。你要把这两项 FAIL 清掉，使同一命令只剩已知 barcode WARN（或全 PASS），以便总管重新开 CA2。

你不回答 PCC 为什么是 0.65，不审 CA2 六项，不改划分/z-score，不训练。

#### 2. 操作规范与边界

必读：`AGENTS.md`、`.agents/skills/pfmval-governance/SKILL.md`、`project_state/current_state.json` 中 `source_commit` / `source_hashes` / `blocked_actions`。

未绑定 `W###`。这是本地治理维护，不是实验代码写入。禁止：

- 训练、派发、result import/accept
- 修改 MPP 标签、ssGSEA、标准划分、z-score 参数、manifest
- `git clean -fd`、删除真实工作树 `PFMval_new_governed_workspaces\W004` 或 W001–W007 持久目录
- 手改 `current_state.json` / `document_registry.json` 哈希
- `python deploy/pfmval_ops.py state sync --with-docs`（会重扫文档，可能把本地 `03审计报告` 写进登记）
- `git add 03审计报告` 或提交该目录
- 开始 CA2 六项源码审计

允许且必须走官方路径：

1. 预览，不要先改。
   ```powershell
   git status --short --branch
   git rev-parse HEAD
   git worktree list --porcelain
   python deploy/pfmval_ops.py agent start-check --strict
   ```
   确认 FAIL 仍是：
   - `state source hash mismatch: document_registry_sha256`
   - `workflow governance v3 state invalid` 指向 `C:\Users\33696\AppData\Local\Temp\pfmval-w004-governance-f8a1c2f`
   并确认 `D:\AI空间转录病理研究\PFMval_new_governed_workspaces\W004` 仍在 list 中。

2. 只修剪失效 Temp 条目。先说明将执行 `git worktree prune`，再执行。prune 后再次 `git worktree list`：Temp 路径应消失，W004 持久路径必须仍在。若 prune 不够，停止并报告，不要手工删 `.git/worktrees` 乱目录，除非预览证明只剩该 stale gitdir 且用户确认。

3. 同步状态哈希（不要 `--with-docs`）：
   ```powershell
   python deploy/pfmval_ops.py state sync --force-revision
   ```
   预期更新：`project_state/current_state.json` 的 `source_hashes.document_registry_sha256`、`source_commit`、`state_revision`，以及生成视图 `CURRENT_STATE.md`（可能还有 README 状态块、`.claude/next-steps.md`、`.claude/session-brief.md`）。列出实际改动文件，不得夹带实验代码。

4. 重跑：
   ```powershell
   python deploy/pfmval_ops.py agent start-check --strict
   ```
   成功标准：FAIL=0。允许保留 barcode WARN。若仍有与本任务无关的新 FAIL，原样报告，不要顺手扩修。

5. 不要在本对话 commit。把将提交文件清单交给用户批准。清单不得包含 `03审计报告/`。

#### 3. 必须回传的审批报告

在聊天中用下面结构短回传（先结论后证据）。不要把 `03审计报告` 当 Git 交付物。

- 任务：治理门禁修复（start-check 两项 FAIL）
- locus：branch / HEAD / dirty
- prune 前/后 `git worktree list` 要点（Temp 是否消失、W004 是否仍在）
- `state sync --force-revision`：新 `state_revision`、新 `source_commit`、新 `document_registry_sha256`
- 修复后 `start-check --strict` 原文 SUMMARY（PASS/WARN/FAIL 计数）
- 总体判定：`GO`（FAIL=0，可重开 CA2）/ `NO-GO`（仍有 FAIL）
- 下一允许动作（只选一个）：
  - 交回总管，由用户批准是否提交治理文件
  - 仍失败，需要用户看新的 FAIL 列表
- 明确未做：未做 CA2 六项、未训练、未提交 `03审计报告`、未 `--with-docs`

完成后停止。不要自动开 CA2，不要写划分部署方案。
