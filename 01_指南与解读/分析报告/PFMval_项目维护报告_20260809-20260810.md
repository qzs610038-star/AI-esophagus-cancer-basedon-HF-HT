# PFMval 项目维护报告（2026-08-09—2026-08-10）

> 报告状态：`pending_user_review`
>
> 审查基线：`main @ f0b2f700c8b68589a5845cb8a0bb84033ad8ba38`
>
> 状态版本：`202`
>
> 总体结论：`CONDITIONAL GO`
>
> 范围：本地非代码治理、本地代码与 Git/工作树治理、服务器目录与 Gitee 往返治理、服务器单一机器配置治理，以及部署方案到实施方案的双阶段门。

## 1. 结论摘要

2026-08-09 至 2026-08-10 的本地治理主体已经落地，并通过当前严格门禁：`PASS=8 / WARN=6 / FAIL=0`。已经形成以下能力：本地历史资料与历史代码退出活跃边界；W### 永久编号和实验专属目录；W005 弃用保号；服务器路径分层；一个实验一个持久 W###；attempt 输出外置；Gitee `fetch-not-pull`；原始训练 CSV/TXT 纳入回传；critical/supporting/diagnostic 哈希分层；服务器机器配置收敛到 `configs/server_paths.yaml`。

本结论仅代表“本地治理实现可进入服务器无训练试点”，不代表服务器运行闭环已经验收。尚未完成的 HARD 项是：真实服务器路径和解释器核验、一次无训练 Gitee 双向试点、带被忽略 CSV/TXT 的最小回传闭包、以及新双阶段门的机器可执行化。故本报告给出 `CONDITIONAL GO`，不建议直接开始正式训练。

## 2. 证据边界与依据

### EB1 证据边界（Evidence Boundary）

- 三个引用对话均已按要求调用读取接口，但接口返回 `dynamic tool request failed`，无法取得可审计正文。本报告不依赖对话标题推断事实。
- 对三个治理方向的判断来自仓库内已落账的 directive、执行记录、Git/Registry 现状和活跃规范；引用对话只作为任务来源说明。
- `server_workspace_sync_step1_execution_plan_20260808.md`、`server_workspace_sync_troubleshooting_governance_review_draft_v001_20260808.md`、本地代码治理执行方案和 Git 文件结构全览，均已在 Document Registry 中归为 `historical` 或 `historical_reference`，只用于追溯原方案及偏差，不能覆盖 active 规则。
- 当前服务器维护规范权威为 `project_state/plans/server_maintenance.md`，其 Registry 状态为 `active / normative`。
- `服务器单一机器配置治理方案_20260810.md` 位于被 Git 忽略的部署方案目录，当前未登记进 Document Registry；它是本地待审成果，不应单独替代 active 规范。

### AS2 依据集合（Authority Sources）

| 依据 | 生命周期/角色 | 本报告用途 |
|---|---|---|
| `AGENTS.md` | 固定安全边界 | Gitee-only、W###、双阶段批准门、哈希批准规则 |
| `CURRENT_STATE.md`、`project_state/current_state.json` | 当前状态事实源 | revision 202、当前指令和 WARN |
| `project_state/directives.jsonl` | append-only 指令账本 | DIR-20260809-001～004、DIR-20260810-001～003 |
| `project_state/plans/server_maintenance.md` | active/normative | 当前服务器路径、诊断、回传和哈希规则 |
| `configs/server_paths.yaml` | 机器可读服务器配置 | 服务器路径、Python、Gitee 传输事实 |
| `project_state/workspace_registry.json` | 工作树事实源 | W001～W005 当前生命周期 |
| 两份 0808 服务器方案 | historical/reference | 原方案、实施登记和偏差对照 |
| 两份 0809 本地代码执行记录 | current evidence | 代码迁移、W001～W004 回收和关闭证据 |
| `服务器单一机器配置治理方案_20260810.md` | local review artifact | 最新单一配置改造说明与本地验证 |

## 3. 改动面、实现状态与影响

### LNG1 本地非代码治理（Local Non-code Governance）

状态：`COMPLETED / current`。

- 建立本地 Git 备份与治理基线；活跃指南、组会资料、团队资料采用索引化跟踪边界。
- `historical_file/` 用于本地历史资料并由 Git 忽略；`phase3_qzs_share/`、`codex_memory/` 保持本地忽略，后者不作为项目事实源。
- 未删除本地资料、未改写历史报告正文、未自动推送远端。

影响：活跃事实与本机历史资料分离，降低误把旧文档当执行规范的概率；代价是被忽略资料不能依赖 Git 远端恢复，仍需本地备份纪律。

### LCG2 本地代码与 Git 治理（Local Code and Git Governance）

状态：`COMPLETED_WITH_OPEN_CLOSEOUT_DEBT`。

- 102 个截止日前候选已唯一分类：101 个迁移，`lora_utils.py` 因仍被使用而保留。
- 101 个旧代码退出 main 当前树，在 `historical_code/pre_mpp1_5/` 本地只读保存；未对该历史树计算文件内容哈希，可从迁移前提交恢复。
- `histogene/`、`egnv1/`、`egnv2/` 的活跃 import 扫描为 0；公共指标统一到 `pfmval_core.metrics`。
- 创建 `experiments/workspaces/_template/`，包含 `code/`、`configs/`、`tests/`、结构化日志索引、共享代码清单和 closeout；模板不占 W 编号。
- W004 仅选择性回收 14 个核心模块、5 个测试、4 个小型配置/合同及骨架，未整体合并 44 个独有提交，也未导入或接纳 A002～A004。
- W002、W003 经精确路径批准后移除物理 worktree，并在 Registry 中标为 `tombstoned`；分支、编号和 Git 历史保留。

影响：新实验代码边界更集中、历史代码可恢复、工作树关闭更可审计；当前 main 相对 `origin/main` 为 ahead 12，说明这些本地提交尚未作为本轮操作推送。

遗留：W001 仍受非终态 attempt 事件阻塞；W004 仍需结果对账和代码审查，均不得冒充完成。

### SWG3 服务器工作区与路径治理（Server Workspace Governance）

状态：`COMPLETED_LOCAL / PENDING_LIVE_VERIFICATION`。

- `configs/server_paths.yaml` 已登记：
  - `server_governance_checkout` → `D:\AIPatho\qzs\pfmval_governance`
  - `server_experiment_worktrees` → `D:\AIPatho\qzs\pfmval_automation`
  - `server_experiment_runs` → `D:\AIPatho\qzs\pfmval_experiment_runs`
  - `server_result_returns` → `D:\AIPatho\qzs\pfmval_result_returns`
  - `server_diagnostics` → `D:\AIPatho\qzs\pfmval_diagnostics`
  - `server_runtime_bundles` → `D:\AIPatho\qzs\pfmval_runtime_bundles`
- `pfmval_deploy_git` 已降为 `legacy / no_new_outputs`；新源码、run、return、diagnostic 不再回流该混合目录。
- `ensure_job_worktree()` 已从按 job 建长期源码目录改为按 `workspace_id` 复用持久 `W###`；attempt 输出放到工作树外。
- 服务器同步规则统一为 exact `git fetch`，禁止隐式 `pull`、merge、rebase 和 force push。
- W005 已标为 `abandoned_reserved`：编号永久保留，无正式 experiment，禁止训练、lease 和执行；后续新实验从 W006 或更高编号登记。

影响：代码、运行输出、回传与诊断的写入边界清晰，减少脏工作树和 job 间交叉污染。路径登记只证明本地配置存在，不证明服务器实物已创建或可写。

### RTR4 排障与结果回传（Rapid Troubleshooting and Return）

状态：`COMPLETED_LOCAL / PENDING_GITEE_PILOT`。

- 诊断仅允许固定 command id、Gitee-only 和非证据输出；不得携带任意 shell、训练参数或结果导入。
- `return_profile_v1.schema.json` 已在本地创建，定义成功、fit 前失败、fit 后失败和 diagnostic 的终态回传矩阵。
- 会产生预测的成功作业必须回传每个实际评估 split 的逐样本原始预测；训练 CSV/TXT、resolved config/argv、started/terminal receipt 按 profile 返回。
- 发布器对 manifest 要求的被忽略文件做精确 force-add，并比较 expected/staged/remote tree；补传使用新不可变 revision，不覆盖旧回传。
- 当前 schema 文件仍为未跟踪文件，尚未进入 Git commit/Gitee，因此服务器无法仅凭现有远端取得完整实现。

影响：回传目标从“有结果包”提升为“身份、终态、必传原始文件和远端树闭包完整”。在 schema 尚未提交并完成往返试点前，只能视为本地实现。

### HTG5 哈希分层治理（Hash-tier Governance）

状态：`COMPLETED_LOCAL_WITH_DECLARED_TRADEOFF`。

- critical：source commit、关键合同、关键输入、metrics/selection proof、原始预测和 bundle integrity 继续使用 SHA-256 HARD。
- supporting：默认使用必传清单、精确大小、归一化记录和 Git tree 闭包，不逐文件计算 SHA-256。
- diagnostic：使用 allowlist、路径边界、大小、UTF-8/LF 和 Git tree 闭包，不作为 accepted metrics 来源。
- supporting/diagnostic 若升级为科学证据，必须重新按 critical 打包和校验。

影响：降低日志、训练历史和诊断文本因逐文件哈希造成的回传阻塞；代价是失去同大小单文件字节篡改的独立检测，依靠不可变 Git object、exact ref 和树闭包补偿。此次放松已有 DIR-20260810-002 的明确授权，不扩展到 critical 证据。

### SMC6 服务器单一机器配置（Server Machine Configuration）

状态：`COMPLETED_LOCAL_PENDING_USER_REVIEW`。

- `configs/server_paths.yaml` 已升级为 Schema 2.0，成为唯一人工维护的服务器机器配置；新增 `runtime.python_interpreter` 和 `transport`。
- 标准服务器解释器为 `C:\Users\AIPatho1\pfmval_env\Scripts\python.exe`；禁止 PATH 猜测。
- 根目录和 `configs/` 下的 `config.server.yaml` 均改为 fail-closed legacy 指针。
- `PFMVAL_CONFIG`、`PFMVAL_PYTHON` 被解析器明确拒绝；主要训练 launcher 改由 `resolve_server_python.ps1` 读取统一 profile。
- 患者、交叉验证和科学协议继续由 `config.yaml` 或对应 critical contract 管理，不混入机器配置。

影响：机器路径、Python 和 Gitee 传输不再多点维护；旧环境变量会直接失败，服务器首次切换前必须清理旧变量并核验解释器实物。

### DPI7 部署方案到实施方案流程（Deployment Plan to Implementation Plan）

状态：`RULES_COMPLETED / MACHINE_ENFORCEMENT_PENDING`。

- 新实验部署讨论稿仅允许放在 `01_指南与解读/部署方案/`。
- 用户批准最终部署方案后，才能创建 `project_state/implementation_plans/W###-<工作树名>_实施方案.md`。
- 实施方案须二次明确批准，之后才能创建/绑定 W###、建立代码目录和修改前配置；用户显式切换 W### 后才能编辑代码。
- 每个条目完成后须在实施方案追加 `completed_pending_user_review`、commit、测试和文件证据。
- 当前 `project_state/implementation_plans/` 尚不存在；未发现对应 schema、CLI 扫描器或阻断式自动门禁。canonical Skills 已完成服务器路径/回传治理适配，但按用户决定，尚未同步新增的双阶段实施方案门。

影响：人工规则已经清楚，但自动化仍可能绕过实施方案二次批准。新实验只能由 Agent 和用户按文档人工守门，不能声称机器已强制执行。

### ADF8 Agent 与文档入口（Agent and Documentation Frontends）

状态：`PARTIALLY_COMPLETED`。

- `.agents/skills/` 继续作为 canonical Skill 权威源；`.claude/skills/` 保持薄适配器。
- `CLAUDE.md` 已收敛为仅引用 `@AGENTS.md`，但该文件受 `.gitignore` 管理，只是本地入口。
- server governance 相关的 `train`、`sync-server`、`post-train`、`experiment-log`、`pfmval-governance`、`pfmval-audit` 已在 DIR-20260810-002 下同步持久 W###、回传和哈希分层规则。
- 双阶段部署/实施方案的新门禁尚未写入上述 Skills，符合用户“skill 修改暂缓”的决定。

## 4. 与原方案的主要偏差

| 原方案/原状态 | 当前实现 | 偏差判断与影响 |
|---|---|---|
| 0808 方案以 `pfmval_deploy_git` 为混合入口，服务器路径尚待拆分 | 已拆为治理 checkout、持久 W###、run、return、diagnostic、runtime bundle；旧目录降为 legacy | 正向收敛；历史方案不能再作为执行路径依据 |
| 原实现按 job/diagnostic id 创建长期源码 worktree | 当前按实验 W### 持久绑定，attempt 输出外置；diagnostic 使用短期 detached source | 已纠正，符合“一实验一工作树” |
| 原方案仅笼统要求日志/预测回传，普通 `git add` 可能漏掉忽略文件 | 已增加终态 return profile、原始 CSV/TXT、精确 force-add 和树闭包 | 本地能力已实现，但 schema 未跟踪且未完成远端试点 |
| 原哈希实现对普通 artifact 与诊断逐文件 SHA | critical 保持 SHA；supporting/diagnostic 改为 inventory/size/Git closure | 经批准的效率优化；存在已声明的单文件字节检测能力下降 |
| W005 曾作为候选服务器实验工作树 | W005 改为 `abandoned_reserved`，不登记正式 experiment、不训练；新实验用 W006+ | 与早期方案偏离但符合最新用户指令 |
| 服务器机器配置分散在两个 `config.server.yaml`、环境变量和脚本 | `configs/server_paths.yaml` Schema 2.0 成为唯一机器配置，旧文件 fail-closed | 正向收敛；旧入口的现场环境变量必须清理 |
| 编号工作树协议第 1 节仍写“当前实现尚未具备该能力” | 持久 W###、外置 run、回传 profile 等已有本地实现 | 文档前言与后续实施登记不一致，应在后续修订中改为“本地已实现、现场待验” |
| 新实验过去可从 plan 直接进入工作树 | 现在增加部署方案批准、实施方案创建、实施方案二次批准、显式切换四层门 | 规则已写入三份 active/approved 文档，机器门禁和 Skill 同步尚未实现 |

## 5. 当前未实现内容与必要后续

### HBL1 严格阻断项（Hard Blocks）

1. 在正式训练前，以 W006 或更高新登记实验完成一次无训练 Gitee 往返试点：exact fetch、持久 W### 绑定、allowlisted diagnostic、最小 return、原始 CSV/TXT 精确 force-add、remote tree 闭包。
2. 通过 Gitee diagnostic 核验 `server_governance_checkout`、`server_experiment_worktrees`、`server_experiment_runs`、`server_result_returns`、`server_diagnostics`、`server_runtime_bundles` 的真实存在性、类型和边界；特别是 `server_diagnostics` 标记了 `live_verification_required`。
3. 核验服务器 `C:\Users\AIPatho1\pfmval_env\Scripts\python.exe` 的真实存在、版本和依赖；本地 Schema 通过不能替代服务器现场核验。
4. 将 `return_profile_v1.schema.json` 与相关实现形成受审提交并经 Gitee 到达服务器；当前未跟踪状态不满足远端可重现性。
5. 新实验必须先完成部署方案批准，再创建并二次批准实施方案；在机器门禁完成前须人工逐项回执，不得直接注册/创建工作树后写代码。

### NBL2 非阻断遗留（Non-blocking Backlog）

- W001 非终态 attempt 事件修复与关闭。
- W004 A002～A004 的补传、import/accept 决策和 closeout。
- `RA2_RETIRE_HOLD_20260809` 隔离区永久删除，以及 HC3 tar/旧 dispatch/旧部署目录的精确退役；均需另行批准。
- 五个 accepted legacy result 缺完整 envelope；它们产生 WARN，但不改变已接受历史结果。
- MPP standardized labels 存在重复/冲突 barcode；重建受保护资产必须另开批准任务。

### CFG3 仍存在的活跃配置/入口矛盾（Configuration Gaps）

1. `01_指南与解读/部署方案/服务器路径索引_20260701.md` 是 AGENTS 必读人工索引，但仍把 `D:\AIPatho\qzs\pfmval_governance\configs\config.server.yaml` 写成“当前服务器配置”，并保留旧根配置核对说明；这与唯一机器配置 `configs/server_paths.yaml` 冲突。建议后续只修改该 active 阅读入口，将两项标为 retired pointer，并指向 governance checkout 下的 `configs/server_paths.yaml`。
2. `deploy/setup_server.ps1` 仍包含 OpenSSH 安装、`pfmval_histogene` Conda 环境和旧 `D:\PFMval_new` 默认目录。它虽然已停止设置 `PFMVAL_PYTHON`，但尚未统一到新的 profile。建议后续明确选择“退役并 fail-closed”或“改为只从 `server_paths.yaml` 读取”；在此之前不得作为新服务器标准安装入口。
3. `deploy/cmd_server.py` 文档标注“历史实现，已禁用”，但源码仍保留可启动 HTTP 服务和旧解释器白名单；`deploy/ssh_config` 仍包含 SSH 操作示例。固定 Gitee-only 边界已使其不具 active 权威，但为防误用，建议后续在精确审核后增加 fail-closed 入口或迁入明确历史区。
4. `deploy/SYNC_GUIDE.md` 中的旧 Miniconda/GitHub/手工同步说明已登记为 `superseded`，仅作历史资料，不构成当前矛盾，无需为本轮治理修改。

## 6. 验证结果

### VAL1 最简验证（Validation）

| 检查 | 结果 | 解释 |
|---|---|---|
| 当前严格门禁 | `PASS=8 / WARN=6 / FAIL=0` | 本报告创建前重新只读执行 |
| 服务器单一配置定向测试 | `3 passed` | Schema、路径解析、旧配置拒绝 |
| PowerShell 解释器/路径解析 | 通过 | 可解析治理 checkout；旧环境变量被拒绝 |
| 本地代码治理测试 | 13 passed、5 passed、1 passed | 模板、资产、指标兼容和策略验证 |
| W001～W004 回收治理测试 | `68 passed` | Phase 3、结果接口和 close-preview |
| 服务器回传治理核心测试 | `101 passed` | 来自 DIR-20260810-002 完成登记 |
| 真实服务器连接/训练/推送/result import | 未执行 | 本轮明确不在授权范围 |

当前 6 项 WARN 为五个旧 accepted result 缺完整 envelope，以及 MPP 标准标签重复/冲突；均不是本次单一配置治理新增。

## 7. 审查建议

建议用户按以下边界验收本轮维护：

- 可验收为完成：本地非代码边界、本地历史代码迁移、W### 模板与 W002/W003 tombstone、W005 弃用保号、服务器路径与回传本地实现、哈希分层、单一机器配置本地改造。
- 仅验收为待现场验证：服务器新路径、服务器 Python、持久 W### 实物、Gitee diagnostic/return 双向闭包。
- 暂不验收为完成：正式服务器训练闭环、双阶段门机器化、未跟踪 return profile 的远端可用性、旧 setup/HTTP/SSH 入口彻底退役、W001/W004/HC3 收口。

在 HBL1 五项完成前，可以继续讨论和批准新部署方案，也可以创建经批准的实施方案；不得跳过二次批准直接创建实验工作树、开始代码编辑或正式服务器训练。

## 8. 本报告写入边界

本次任务只新增本报告文件。未修改、暂存、提交、推送或删除其他项目文件；未连接服务器、未训练、未导入或接纳结果，也未新增哈希计算。
