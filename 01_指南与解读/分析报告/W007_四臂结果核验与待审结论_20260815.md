# W007 四臂结果核验与待审结论

> 状态：`draft_pending_user_review`
>
> 证据级别：`pending_review`，尚未通过 `result import`，不得写作 accepted 结论
>
> 实验：`mpp2_cpgcr_probe_v001_20260811`
>
> 事实截止：2026-08-15

## 1. 核验范围

本报告只核验既有 FBR、RCC、HCR、CPGCR 四臂结果，不启动训练、不根据 XZY 选择配置。

| 臂 | Return ref | Return commit | Attempt | Source commit |
|---|---|---|---|---|
| FBR | `automation/server/W007/FBR-R001` | `fe8771f99472cf0a6d99061f50ca7321c4260f1f` | 基线复放 | `7be042a27d7a914744dfca1969ecae9ed6a3fb2d` |
| RCC | `automation/server/W007/A019` | `036c7139fd7792055692e4082b23cb6380f116d4` | `A019` | `24a87c72c90122fb061ef6f93ad133a04a3ab3d1` |
| HCR | `automation/server/W007/A022` | `d91aaa2c8ba560c4f43884560877227c83fb377d` | `A022` | `0f81be3cd542baa19ef3a2fe97bb146122a055bb` |
| CPGCR | `automation/server/W007/A023` | `1519551098ae313f6a0082079de8b045d8e9084f` | `A023` | `0f81be3cd542baa19ef3a2fe97bb146122a055bb` |

核验结果：四臂清单中的 artifact 均存在且大小一致；internal-val 均为 1,078 个 spot、6 名患者，XZY 均为 1,039 个 spot、1 名患者；四臂的样本标识和 truth 列逐项一致。由原始预测表独立重算的指标与回传 JSON 最大绝对差为 `5.37e-8`，属于浮点精度差异。

### 1.1 main 与 W007 commit range 分类

- merge-base 为 `3451db4895e46a9894a7fec9d48ed9bb023cab34`；当前 main 仅多出无关的研究方案 Skill 提交 `1430c476b1f301b2d9ba66fb4ab3386031a52a26`。
- 实验专属代码：`experiments/workspaces/W007/mpp2_cpgcr_probe/`，共 21 个文件、约 3,999 行新增；应在后续独立回收任务中按目录审查，不与治理修复混合。
- 可复用公共治理候选：execution binding/attempt 预算、外置 run/return 路径、local-only 文档门禁相关的 `deploy/pfmval_ops.py`、`scripts/pfmval_governance.py`、`scripts/pfmval_state.py` 及测试。按用户最新指令，本对话不实施。
- 历史排障证据：`automation/diagnostics/`、W007 operation cards、prepared-attempt 版本链和 `project_state/diagnostics.jsonl`，仅用于保留故障—修复—恢复脉络，不作为科学结果。
- 四个 return commits：只进入本地 quarantine 和后续 importer，未 merge/cherry-pick 到 main。

## 2. 正式指标比较

### 2.1 Internal validation（内部验证）

| 臂 | patient-balanced z-MSE ↓ | pooled z-MSE ↓ | z-MAE ↓ | pooled PCC ↑ | raw MAE ↓ | mean pathway raw R² ↑ |
|---|---:|---:|---:|---:|---:|---:|
| FBR | 0.380743 | 0.374886 | 0.454207 | 0.797126 | 884.949 | 0.635124 |
| RCC | **0.378252** | **0.373293** | **0.452587** | **0.798154** | **881.387** | **0.636668** |
| HCR | 0.379641 | 0.373931 | 0.453010 | 0.797706 | 882.226 | 0.636048 |
| CPGCR | 0.378847 | 0.373592 | 0.453023 | 0.798000 | 882.270 | 0.636379 |

RCC 在内部验证的六项汇总指标上均为数值最优，但幅度很小：相对第二名 CPGCR，patient-balanced z-MSE 仅低 `0.000595`，约 `0.157%`。

### 2.2 External XZY benchmark（外部 XZY 基准）

| 臂 | z-MSE ↓ | z-MAE ↓ | pooled PCC ↑ | raw MAE ↓ | mean pathway raw R² ↑ |
|---|---:|---:|---:|---:|---:|
| FBR | 0.662077 | 0.619123 | 0.654896 | 1176.211 | -0.088017 |
| RCC | 0.659534 | 0.619592 | 0.653758 | 1177.107 | -0.087571 |
| HCR | 0.658108 | 0.618486 | **0.655982** | 1174.598 | -0.082654 |
| CPGCR | **0.656832** | **0.618183** | 0.654932 | **1173.902** | **-0.080119** |

- HCR 的 external PCC 最高，比 CPGCR 高 `0.001050`，比 FBR 高 `0.001086`。
- CPGCR 的 external raw MAE 最低，比 HCR 低 `0.696`（约 `0.059%`），比 FBR 低 `2.310`（约 `0.196%`）。
- CPGCR 的 mean pathway raw R² 最高，但仍为负值；相对 HCR 高 `0.002534`，相对 FBR 高 `0.007897`。
- external 方向并不一致：PCC 指向 HCR，z-MSE、z-MAE、raw MAE 和 mean pathway raw R² 指向 CPGCR，RCC 则只在 internal 指标占优。

## 3. checkpoint 与训练过程核对

- FBR 是 accepted baseline checkpoint 复放，不是一次新训练；回传的 replay check 为 PASS，val loss 与参考值绝对差 `3.34e-7`，val PCC 绝对差 `1.82e-7`。
- RCC 的 checkpoint 对应 epoch 5，内部 patient-balanced z-MSE 为 `0.378252`。
- HCR 的 checkpoint 对应 epoch 1；epoch 2 的数值改善仅约 `5.5e-6`，没有超过 `min_delta=0.0001`。
- CPGCR 的 checkpoint 对应 epoch 5；epoch 7 的数值改善约 `9.99e-5`，仍未超过 `min_delta=0.0001`。
- 四臂均在内部 checkpoint 冻结后评估 XZY；本报告没有用 XZY 重选 checkpoint 或提出追加训练。

## 4. 待审结论

建议登记为：

> W007 单 seed（42）四臂探针显示，HCR 的 external pooled PCC 略高，CPGCR 的 external raw MAE 与 mean pathway raw R² 略优，RCC 的 internal patient-balanced z-MSE 最低。各优势幅度很小、指标方向混合，且只有一个 seed；当前证据不支持宣称任一残差臂明确优于其他臂或 FBR。XZY 仅用于合同冻结后的外部基准评估，不得据此追加训练、重选配置或触发重跑。

初步建议：保持四臂结果为“无明确优胜者”的待审证据；不启动新训练。若未来要提高结论强度，应先由用户另行批准多 seed 重复和统计不确定性方案，且仍只用 internal-val 选择，XZY 保持一次性外部评估边界。

## 5. 治理状态与阻塞

当前 `main` 的 W007 Registry 仍为 `planned/preflight/run_limit=1`。四个回传包只有 `return_manifest.json`，而当前 `result import` 要求标准 `result.json`、`automation/jobs/<job_id>/job.json` 和单一 job/source 绑定。四臂又分别对应不同 attempt 与 source commit，因此当前 CLI 无法无损、原子地导入本批次。

本次已停止在 quarantine 验证，未手工修改 Registry。修复方案见：
`01_指南与解读/部署方案/W007四臂结果导入治理缺口修复部署方案_20260815.md`。

## 6. 原始文件位置

本地 quarantine 根目录：

```text
D:\AI空间转录病理研究\PFMval_new\project_state\inbox\W007\quarantine
```

- RCC：`RCC\automation\returns\W007\20260815-004443-507558-formal-batch\RCC\training_history.csv`
- HCR：`HCR\automation\returns\W007\20260815-135800-921822-formal-batch\HCR\training_history.csv`
- CPGCR：`CPGCR\automation\returns\W007\20260815-135800-921822-formal-batch\CPGCR\training_history.csv`
- FBR 没有训练历史，因为它是冻结基线复放；查看 `FBR\...\FBR\baseline_replay_check.json`、`checkpoint.json` 和预测/指标文件。

原始逐样本预测分别为每臂目录下的 `internal_val_predictions.csv` 与 `XZY_predictions.csv`。

## 7. 事实来源

- 根治理边界：`AGENTS.md`、`CURRENT_STATE.md`、`project_state/current_state.json`
- 实验事实根：`experiments/experiment_registry.json`
- W007 最终分支：`1d19bc2108a94a344491c937c2890fedda24e858`
- 实验协议：W007 source `0f81be3cd542baa19ef3a2fe97bb146122a055bb` 中的 `configs/protocol_v001.yaml`
- 四个不可变 return commits：见 §1
