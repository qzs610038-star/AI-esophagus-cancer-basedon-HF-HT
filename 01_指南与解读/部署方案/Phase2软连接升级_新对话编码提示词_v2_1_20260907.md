# Phase2软连接升级：新对话正式编码提示词

将下方“我的任务”至末尾复制到项目的新对话即可。它授权下一对话进行代码实现与必要验收，不要求重新讨论已确定的三点；正式服务器训练由我复制代码包后启动。

## 我的任务

请在当前项目 `D:\AI空间转录病理研究\PFMval_new` 实施Phase2软连接升级v2.1，最终交付一个可以直接复制到Windows服务器的独立代码包。

我已要求保留既有模型主体，采纳三点：训练前核对标签关系与图覆盖；固定第15/25轮比较；本地固定β=1预测平滑对照。请直接推进代码，不重新索取这三点的批准。其余默认值按文件实现；未经我另行指令，不自动搜索超参数或启动正式训练。

### 事实源与交付范围

优先读取这些文件，按需读相关源代码，不全量加载仓库：

1. [最终部署方案v2.1](D:/AI空间转录病理研究/PFMval_new/01_指南与解读/部署方案/Phase2软连接升级_最终部署方案_v2_1_20260907.md)
2. [集中配置草案](D:/AI空间转录病理研究/PFMval_new/01_指南与解读/部署方案/Phase2软连接升级_集中配置_v2_1_20260907.json)
3. [决策与执行记录v2.1](D:/AI空间转录病理研究/PFMval_new/01_指南与解读/部署方案/Phase2软连接升级_决策与执行记录_v2_1_20260907.md)
4. [Fable意见与训练后排查清单](D:/AI空间转录病理研究/PFMval_new/01_指南与解读/分析报告/Phase2软连接升级_Fable意见与训练后排查清单_20260907.md)
5. `AGENTS.md`、`experiments/_template/README.md`、`configs/server_paths.yaml`，以及必要的活动修复登记、MPP2划分和现有独立基线包。

旧v2.0方案、Fable原文与原学习指南保留追溯；遇到旧文档“仍待三项审核”的描述，以本次任务和v2.1为准。模型数学以v2.1为准，实际公共数值以集中配置为准；不一致先定位消歧，不静默改科学定义。

本地包路径：`D:\AI空间转录病理研究\PFMval_new\experiments\phase2_softlink_local_v2`。
服务器代码：`D:\AIPatho\qzs\code\phase2_softlink_local_v2`。
服务器结果：`D:\AIPatho\qzs\runs\phase2_softlink_local_v2\<批次编号>\<运行编号>`。

从独立模板开始，代码、小配置、必要划分与映射放包内；大特征缓存和权重通过配置引用。入口从自身路径解析文件，不依赖仓库根目录或上级Python导入。保留模板的日志、版本与原始结果约定。不要创建工作树、提交/推送、压缩包、计算哈希或自动远端同步；不改团队目录。

### 分工：GPT分配与验收，Cursor编码

GPT负责阅读事实源、提炼接口和验收条件、分配任务、审阅变更、执行最简必要验收与交付说明。主要代码、脚本、测试和修复由Cursor完成；GPT不要自行接管大规模编码。

使用 `C:\Users\33696\.agents\skills\external-agent-router\SKILL.md`，调用Cursor，指定 **Grok 4.6 xhigh fast**。先查询实时模型列表；当前已核对的原始编号是 `cursor-grok-4.6-xhigh-fast`。保留xhigh和fast要求，不能静默换模型或降档。

写代码使用 `external_agent_run_write`，`provider=cursor`、`model=cursor-grok-4.6-xhigh-fast`、`use_worktree=false`。优先直接使用已含推理/速度的原始编号，不再重复拼接可能不受支持的effort/fast参数。若编号被拒绝，先核对实时列表及参数形式；模型确实不可用时说明原因，不偷偷换成其他模型。

分成三批有依赖的任务串行交给Cursor，避免多个写入者重叠：
- 数据与核心计算：集中配置、身份/尺度、固定图、h/z/直接B、支持集与损失、无训练预检。
- 训练与入口：配对随机流、训练/早停/选点、配置快照、总/分入口、运行清单、冻结后预测。
- 分析与交付：全部本地指标、15/25轮比较、固定平滑、Fable排查输出、必要测试和中文README。

每批给出明确文件范围、接口、禁止越界项和验收条件。检查completed、退出码、stderr、改动文件及实际测试证据；发现问题交回Cursor修复，再由GPT复验。不要仅凭“已完成”文字宣布包可运行。

### 必须实现的入口与配置

包根目录提供：

- `run_all.ps1 -Config .\config.json`：默认正式配置从头完成准备、三种子×四臂共12次、内部排序和冻结、12个正式模型的XZY原始预测。
- `run_point.ps1`、`run_relation.ps1`、`run_spatial.ps1`、`run_joint.ps1`：各自只跑一个实验臂，默认全部配置种子，可用 `-Seed 42` 单跑。
- `run_seed.ps1 -Seed 42`：只跑该种子的四臂。
- `run_precheck.ps1`：只生成标签关系与图覆盖报告。
- `run_external.ps1 -Manifest <完整清单>`：明确指定可比的12个正式终点后统一外测。
- `analyze_local.ps1 -BatchDir <回传目录>`：回传后计算全部指标、同轮读数、固定平滑和排查报告。

薄脚本复用同一调度器与训练代码。一次启动冻结公共配置和任务清单，分入口仅改变arm/seed范围；运行过程中改源config不得改变当前批次。每次启动新建目录，保留旧输出；任务失败记录状态并非零退出，总入口不得把部分任务当整批完成，不自动续训或找最新checkpoint。

把集中配置草案落实为包内唯一人工编辑的 `config.json`，集中提供已定义的维数、温度、N/F数量/阈值、batch、关系权重、预热/爬坡、图参数、学习率、早停、种子和路径。各模块从配置取值，不能表面可调、内部仍写死256。关系编码维数与共享维数分别生效。

标明支持的参数范围及中文含义；`graph.use_image_similarity`和`relation.far_patient_scope`按方案实现，正式仍为true/all。不要额外实现稠密Q、RKD或复杂图再冒充默认功能。用户改变参数后使用新config_version/comparison_name和run_kind=exploration；探索总入口只完成该配置训练与内部汇总，不自动拿XZY调参。用户亲自改配置并启动不必重复请求相同批准。

### 科学与记录要求

固定MPP2、冻结1536维UNI2-h、30维训练标准化ssGSEA；默认h/z均256。保留原Q/P、同slide/split一跳图、直接B零初始化及四臂配对训练规则。真实标签不进入预测接口。内部患者宏平均PCC与平分MSE的聚合层级必须正确；外部不用于选点或调参。

配置null路径、slide身份和修复参数按真实来源补齐；服务器路径只从本项目配置主表和经核实补登记项取。缺失事实明确列出，不回退旧标签、不以patient_id代替未经核实的slide_id、不用虚构输入通过验收。能独立实施的部分先完成。

落实三项补充：
- 预检使用原阈值、独立seed20260908、完整训练中心打乱3遍；正式随机流不受影响。图预检只用训练/内部验证。低有效率等是WARN，不自动改参数或停下来索要逐项批准。
- 固定第15/25轮读数来自history，不增加训练轮数或外测checkpoint；故障/缩短预算导致缺失时明确标注。
- 对point臂三个正式内部预测均做β=1同图平滑，单独存本地analysis，不拟合β、不覆盖raw、不计为第五臂或候选选择；按既定度分组描述并标明有效点集。

服务器只产训练、选点必需指标及原始输出；完整派生指标与图表在本地。记录实际配置、版本/源码快照、输入尺度身份、关系抽样、图边权、运行清单、checkpoint选择和原始预测。Fable全部假设保留在包内docs与后续排查报告中；未实测不能写成真实失败原因。

### 最小验收与最终交付

由GPT独立核对Cursor实现，并运行能发现关键问题的少量检查：关系支持/分母与梯度、图分批一致性/空邻域、真实小输入身份尺度与逆变换、非默认维数的合成前向、β=1平滑、总/分入口任务范围和配置冻结、包外工作目录运行。

调度验收使用明确标记的无训练替身，不制造正式结果；不执行优化器更新、性能试训或服务器正式训练。数据不齐时区分已验证、未验证与待补事实，不谎称真实数据验收通过。科研有效性错误应修正，普通WARN不扩展为新治理门禁。

最终给我：
1. 准确的可复制代码包文件夹路径。
2. 总入口、分入口及回传后本地分析的实际命令。
3. 唯一配置文件位置、常用参数中文解释、一个单参数探索例子。
4. GPT实际验收结果、尚未核实的输入/环境与影响；区分代码完成和训练未运行。
5. 修改/执行记录以及训练后如何依据Fable清单分析结果的说明。

我随后手动复制包到服务器，运行训练测试并回传结果。

