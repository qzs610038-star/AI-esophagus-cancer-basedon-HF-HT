# 服务器路径索引_20260701

> 当前机器可读路径事实源：`configs/server_paths.yaml`。本文是 2026-07-01 人工快照；qzs/cache 使用前必须实时核验。服务器当前只通过 Gitee 交换代码、状态和小型结果。

> 依据本地保存的服务器目录清单整理：
> `folder_inventory_data.txt`（生成于 2026-07-01 10:59:43）和
> `folder_inventory_qzs.txt`（生成于 2026-07-01 10:57:27）。
> 缓存目录补充依据：`folder_inventory_cache.txt`（生成于 2026-07-01 11:16:20）。

## 维护原则

- `D:\AIPatho\Patch\visiumhd_patch` 是独立数据目录，短期内预计不会变化。后续训练脚本可以把这里的 MPP 划分数据路径视作相对稳定的事实来源。
- `D:\AIPatho\shared\.cache` 是共享缓存目录，短期内通常只在模型下载、torch hub 加载、缓存清理时变化。涉及 Hugging Face / torch hub / 预训练权重加载的服务器工作必须先读本文缓存章节。
- `D:\AIPatho\qzs` 是个人工作目录，并参与代码 push/pull 同步，短期内会随着提交、拉取、训练结果回收而变化。使用其中的代码、checkpoint、cache 路径前，优先重新生成或核对最新 `folder_inventory_qzs.txt`。
- 两份清单的采集阈值为“单目录直接文件数 > 200 时省略文件明细”。因此 patch 图片、token cache 等大目录只记录目录路径和文件数量，不逐个列出文件名。
- 本文是服务器路径索引，不替代运行前检查。正式训练前仍需执行 `python scripts/check_project_state.py --mode server` 或对应 preflight。

## 路径总览

| 类型 | 路径 | 稳定性 | 用途 |
|---|---|---:|---|
| 外部 MPP 数据根 | `D:\AIPatho\Patch\visiumhd_patch` | 高 | 5 种 MPP 划分的 patch、h5ad、ssGSEA 文件 |
| 共享缓存根 | `D:\AIPatho\shared\.cache` | 中 | Hugging Face hub、torch hub、预训练模型缓存 |
| 个人工作根 | `D:\AIPatho\qzs` | 中低 | 个人同步目录、数据副本、工具、历史部署包 |
| 当前 Git 工作副本 | `D:\AIPatho\qzs\pfmval_deploy_git` | 中低 | 推荐服务器项目代码目录 |
| Phase2 数据副本 | `D:\AIPatho\qzs\data-phase2` | 中 | 3 患者 patch 与 ssGSEA / zscore 标签 |
| 当前服务器配置 | `D:\AIPatho\qzs\pfmval_deploy_git\configs\config.server.yaml` | 中低 | 标准服务器训练配置 |
| 根目录服务器配置副本 | `D:\AIPatho\qzs\pfmval_deploy_git\config.server.yaml` | 中低 | 旧/兼容配置位置，使用前需和 `configs\` 下版本核对 |

## 可直接引用的关键路径

### 项目与同步

```text
D:\AIPatho\qzs
D:\AIPatho\qzs\pfmval_deploy_git
D:\AIPatho\qzs\pfmval_deploy_git\configs\config.server.yaml
D:\AIPatho\qzs\pfmval_deploy_git\config.server.yaml
```

服务器训练优先在 `D:\AIPatho\qzs\pfmval_deploy_git` 下执行。`D:\AIPatho\qzs\pfmval_deploy` 是较旧部署目录，保留作历史参考，不建议作为新实验默认工作目录。

### Phase2 三患者数据

```text
D:\AIPatho\qzs\data-phase2\patch
D:\AIPatho\qzs\data-phase2\ssGSEA
D:\AIPatho\qzs\data-phase2\ssGSEA_zscore
```

| 患者 | 原始 patch 目录 | split 目录 | train | val | 标签 |
|---|---|---|---:|---:|---|
| HYZ15040 | `D:\AIPatho\qzs\data-phase2\patch\HYZ15040_noov` | `D:\AIPatho\qzs\data-phase2\patch\HYZ15040_noov_split` | 2390 | 265 | `D:\AIPatho\qzs\data-phase2\ssGSEA_zscore\HYZ15040_ssGSEA_zscore.csv` |
| JFX0729 | `D:\AIPatho\qzs\data-phase2\patch\JFX0729_noov` | `D:\AIPatho\qzs\data-phase2\patch\JFX0729_noov_split` | 7010 | 778 | `D:\AIPatho\qzs\data-phase2\ssGSEA_zscore\JFX0729_ssGSEA_zscore.csv` |
| LMZ12939 | `D:\AIPatho\qzs\data-phase2\patch\LMZ12939_noov` | `D:\AIPatho\qzs\data-phase2\patch\LMZ12939_noov_split` | 6762 | 751 | `D:\AIPatho\qzs\data-phase2\ssGSEA_zscore\LMZ12939_ssGSEA_zscore.csv` |

JFX 旧数据仍存在：

```text
D:\AIPatho\qzs\data-phase2\patch\JFX0729_noov_old
D:\AIPatho\qzs\data-phase2\patch\JFX0729_noov_split_old
```

默认不要用于新训练，除非做历史对照或修复验证。

### Phase2 缓存与结果

```text
D:\AIPatho\qzs\pfmval_deploy_git\uni2h_cache_tokens\HYZ15040\train
D:\AIPatho\qzs\pfmval_deploy_git\uni2h_cache_tokens\HYZ15040\val
D:\AIPatho\qzs\pfmval_deploy_git\checkpoints\online_cls
D:\AIPatho\qzs\checkpoints\online_cls
```

当前清单只确认 `uni2h_cache_tokens\HYZ15040` 下存在 train/val token cache。JFX0729、LMZ12939 的 token cache 在本次 qzs 清单中未作为已存在路径出现，启动依赖 token cache 的重跑前应重新核对或重建。

## 共享缓存目录

缓存根：

```text
D:\AIPatho\shared\.cache
```

缓存清单摘要：90 个目录、237 个已列文件、0 个省略文件、0 个大目录、0 个扫描错误。

### Hugging Face cache

```text
D:\AIPatho\shared\.cache\huggingface
D:\AIPatho\shared\.cache\huggingface\hub
D:\AIPatho\shared\.cache\huggingface\xet
```

| 项目 | 路径 | 状态 |
|---|---|---|
| Hugging Face token 文件 | `D:\AIPatho\shared\.cache\huggingface\token` / `stored_tokens` | 敏感凭据文件，只记录路径，禁止复制内容到文档或对话 |
| UNI2-h Hub 缓存 | `D:\AIPatho\shared\.cache\huggingface\hub\models--MahmoodLab--UNI2-h` | 已存在 |
| UNI2-h snapshot | `D:\AIPatho\shared\.cache\huggingface\hub\models--MahmoodLab--UNI2-h\snapshots\d517a8dd47902dd7c308b3c36f63bce47e7b9a43` | 含 `config.json` 和 `pytorch_model.bin` |
| UNI2-h 权重文件 | `D:\AIPatho\shared\.cache\huggingface\hub\models--MahmoodLab--UNI2-h\snapshots\d517a8dd47902dd7c308b3c36f63bce47e7b9a43\pytorch_model.bin` | 约 2.54 GiB |
| UNI2-h incomplete blob | `D:\AIPatho\shared\.cache\huggingface\hub\models--MahmoodLab--UNI2-h\blobs\6e077eda234bebc595868d918d3458d9dd32a050199b0ff04443b2f46a0a3b1e.incomplete` | 0 bytes，疑似未完成下载残留 |
| OpenMidnight Hub 目录 | `D:\AIPatho\shared\.cache\huggingface\hub\models--SophontAI--OpenMidnight` | 目录存在但当前清单显示为空 |

涉及 `HF_HOME` / `TRANSFORMERS_CACHE` / `HF_HUB_OFFLINE` 的服务器命令，优先指向或核对 `D:\AIPatho\shared\.cache\huggingface`。

### Torch cache

```text
D:\AIPatho\shared\.cache\torch
D:\AIPatho\shared\.cache\torch\hub
D:\AIPatho\shared\.cache\torch\hub\checkpoints
```

| 项目 | 路径 | 状态 |
|---|---|---|
| DenseNet121 权重 | `D:\AIPatho\shared\.cache\torch\hub\checkpoints\densenet121-a639ec97.pth` | 已存在，约 30.8 MiB |
| ResNet18 权重 | `D:\AIPatho\shared\.cache\torch\hub\checkpoints\resnet18-f37072fd.pth` | 已存在，约 44.7 MiB |
| DINOv2 torch hub repo | `D:\AIPatho\shared\.cache\torch\hub\facebookresearch_dinov2_main` | 已存在，含 `hubconf.py`、`dinov2\`、docs、notebooks、scripts |

涉及 `TORCH_HOME` 或 `torch.hub.load()` 的服务器命令，优先核对 `D:\AIPatho\shared\.cache\torch`。

### 缓存环境变量建议

```powershell
$env:HF_HOME = "D:\AIPatho\shared\.cache\huggingface"
$env:TORCH_HOME = "D:\AIPatho\shared\.cache\torch"
$env:HF_HUB_OFFLINE = "1"
```

只有在确认需要联网下载/补齐模型时，才临时取消 `HF_HUB_OFFLINE=1`。不要把 `huggingface\token` 或 `stored_tokens` 的内容写入日志、Markdown、commit、聊天记录。

## MPP 数据根

外部数据根：

```text
D:\AIPatho\Patch\visiumhd_patch
```

目录结构模式：

```text
D:\AIPatho\Patch\visiumhd_patch\{mpp_id}\{patient}\
D:\AIPatho\Patch\visiumhd_patch\{mpp_id}\{patient}\patch_images
D:\AIPatho\Patch\visiumhd_patch\{mpp_id}\{patient}\{patient}_ssGSEA.csv
D:\AIPatho\Patch\visiumhd_patch\{mpp_id}\{patient}\{patient}_scored.h5ad
D:\AIPatho\Patch\visiumhd_patch\{mpp_id}\{patient}\aggregated_patches.h5ad
```

重要提醒：MPP 数据根中的 `{patient}_ssGSEA.csv` 当前是原始 ssGSEA 分数，尚未做 z-score 标准化。任何训练前必须先生成标准化标签；标准化参数应只在训练集上拟合，再应用到内部验证集和外部 XZY 测试集，避免标签分布泄漏。禁止直接使用原始 `{patient}_ssGSEA.csv` 启动正式训练。

患者集合：

```text
HYZ15040, JFX, LMZ12939, TGC, XSL, XZY, ZHZ
```

### patch_images 数量

| MPP | HYZ15040 | JFX | LMZ12939 | TGC | XSL | XZY | ZHZ |
|---:|---:|---:|---:|---:|---:|---:|---:|
| 1 | 3071 | 7788 | 7275 | 1116 | 1526 | 1039 | 1074 |
| 2 | 1454 | 2029 | 3351 | 1116 | 1526 | 1039 | 1074 |
| 3 | 5807 | 8116 | 13376 | 4476 | 6093 | 4139 | 4257 |
| 4 | 429 | 541 | 889 | 308 | 396 | 269 | 282 |
| 5 | 1722 | 2162 | 3549 | 1206 | 1599 | 1082 | 1132 |

### MPP 训练脚本填路径模板

```powershell
$DataRoot = "D:\AIPatho\Patch\visiumhd_patch"
$MppId = "1"
$Patient = "HYZ15040"
$PatientDir = Join-Path $DataRoot "$MppId\$Patient"
$PatchDir = Join-Path $PatientDir "patch_images"
$LabelCsv = Join-Path $PatientDir "${Patient}_ssGSEA.csv"
```

JFX 在 MPP 数据根中的患者名是 `JFX`，不是 Phase2 中的 `JFX0729`。写脚本时要显式处理这个命名差异。

## qzs 顶层目录快照

| 目录 | 作用 | 备注 |
|---|---|---|
| `D:\AIPatho\qzs\checkpoints` | 根级训练结果 | 已有 `online_cls` 结果 |
| `D:\AIPatho\qzs\data-phase2` | 3 患者数据副本 | 含 patch、ssGSEA、ssGSEA_zscore |
| `D:\AIPatho\qzs\data-phase3` | 预留目录 | 当前为空 |
| `D:\AIPatho\qzs\deploy` | 远程命令服务文件 | 含 `cmd_server.py` |
| `D:\AIPatho\qzs\pfmval_deploy` | 历史部署包解压目录 | 不作为新实验默认入口 |
| `D:\AIPatho\qzs\pfmval_deploy_git` | 当前 Git 工作副本 | 新实验优先使用 |
| `D:\AIPatho\qzs\tools` | 辅助工具 | 含代理/隧道工具，非训练数据 |

qzs 清单摘要：104 个目录、343 个已列文件、54243 个因大目录阈值被省略的文件、14 个大目录、0 个扫描错误。

data 清单摘要：76 个目录、106 个已列文件、96308 个因大目录阈值被省略的文件、35 个大目录、0 个扫描错误。

## 更新流程

当 data 目录更新时：

```powershell
cd D:\AIPatho\Patch\visiumhd_patch
powershell -NoProfile -ExecutionPolicy Bypass -File .\inventory_folder.ps1 -Output folder_inventory_data.txt
```

当 qzs 目录经过 pull/push、训练结果回收、缓存重建后：

```powershell
cd D:\AIPatho\qzs
powershell -NoProfile -ExecutionPolicy Bypass -File .\inventory_folder.ps1 -Output folder_inventory_qzs.txt
```

当共享缓存目录经过模型下载、缓存清理或离线迁移后：

```powershell
cd D:\AIPatho\shared\.cache
powershell -NoProfile -ExecutionPolicy Bypass -File .\inventory_folder.ps1 -Output folder_inventory_cache.txt
```

建议把新的 txt 清单同步回本地后，再更新本文。短期内优先更新 qzs 清单；cache 清单在模型下载/清理后更新；data 清单只有在外部数据实际增删或重新切分后才需要刷新。
